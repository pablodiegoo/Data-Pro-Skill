---
phase: 03-qualitative-injection
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [SKILL.md]
autonomous: true
requirements: [MODE-02]

must_haves:
  truths:
    - "User can type /dps-mode:quali and from that point, all subsequent commands — /dps-cross, /dps-execute, any analysis — automatically include qualitative enrichment from available open-ended data"
    - "The Anthropologist stage (Stage 3) activates in the agent loop for every command when quali mode is active per D-08"
    - "User can deactivate quali mode by running /dps-mode:quant to return to the default 3-stage loop (Stat → Critic → Tufte)"
    - "When /dps-mode:quali is used as a prefix (e.g., /dps-mode:quali /dps-cross VarX x VarY), the Anthropologist activates for that single command only"
  artifacts:
    - path: "SKILL.md"
      provides: "Full /dps-mode:quali implementation replacing stub"
      contains:
        - "### /dps-mode:quali"
        - "session-scoped toggle"
        - "Behavioral changes when quali mode is active"
        - "Anthropologist stage (Stage 3) activates"
        - "desativar com /dps-mode:quant"
  key_links:
    - from: "/dps-mode:quali toggle"
      to: "Agent loop Stage 3 Anthropologist activation condition"
      via: "Mode state read by agent loop at command invocation"
      pattern: "/dps-mode:quali.*active"
    - from: "/dps-mode:quali prefix usage"
      to: "Single-command Anthropologist activation"
      via: "Prefix detection before agent loop execution"
      pattern: "/dps-mode:quali.*prefix.*single command"
---

## Phase Goal

**As a** market researcher, **I want to** toggle a persistent qualitative analysis mode that enriches every subsequent command's output with theme extraction, verbatim evidence, and segment-mapped qualitative findings, **so that** I can weave qualitative insights into all my quantitative analyses without manually running /dps-inject-open for each cross or execute run.

---

<objective>
Expand the `/dps-mode:quali` stub in SKILL.md (currently 4 lines: lines 330-333) into a full session-scoped mode toggle implementation following the pattern established by `/dps-mode:quant` (lines 317-329). The mode activates the Anthropologist stage (Stage 3) in the agent loop for all subsequent commands, making qualitative enrichment persistent across the session.

Purpose: Give the researcher the ability to work in a qualitative-first analytical mode where every command's output is enriched with verbatim themes, archetypes, and segment-mapped qualitative findings — without requiring explicit `/dps-inject-open` invocation for each analysis.

Output: SKILL.md updated with full `/dps-mode:quali` specification including behavioral changes, usage instructions (standalone vs prefix), deactivation, and cross-references to agent definitions.
</objective>

<execution_context>
@/home/pablo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pablo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@SKILL.md
@agents/agent-anthropologist.md
@agents/agent-critic.md
@agents/agent-tufte-designer.md
@.planning/phases/03-qualitative-injection/03-CONTEXT.md

<interfaces>
<!-- From SKILL.md — mode toggle pattern and insertion anchor -->
Current `/dps-mode:quali` stub (lines 330-333) to be replaced:
```
### /dps-mode:quali

Activates **Anthropologist** persona. Focus: latent needs, sentiment analysis, consumer archetypes, journey mapping, verbatim extraction. The Anthropologist reads `agents/agent-anthropologist.md` for thematic categorization, frequency notation (never percentages when N < 30), and qualitative-to-quantitative segment mapping. Use when the user provides open-ended responses, interview transcripts, or focus group notes. **All qualitative output attaches to segments defined in the `/dps-setup` manifesto.**
```

Pattern to follow: `/dps-mode:quant` (lines 317-329) which defines:
- Session-scoped toggle statement
- "Behavioral changes when quant mode is active" with 4 enumerated behaviors
- Usage section (standalone vs prefix)
- Reference to agent definitions

After Plan 03-01 completes, the agent loop will have:
- Stage 3: Anthropologist with activation condition: "runs when (a) /dps-inject-open, OR (b) /dps-mode:quali is active (see Modes section below)"
- Stage 4: Tufte Designer (synthesizes Anthropologist output alongside other stages)

The /dps-mode:quali implementation activates condition (b), making the Anthropologist stage persistent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand /dps-mode:quali stub into full session-scoped mode toggle</name>
  <files>SKILL.md</files>
  <action>
Replace the existing `/dps-mode:quali` stub at lines 330-333 with the full implementation. The replacement follows the exact pattern of `/dps-mode:quant` (lines 317-329) — same structure, same level of detail, same usage instructions format. Maintain the existing heading level (`### /dps-mode:quali`).

**Replacement content:**

```markdown
### /dps-mode:quali — Anthropologist Persona

A session-scoped toggle. Once activated, all subsequent commands in the current session operate in quali mode — the Anthropologist stage (Stage 3) activates in the invisible agent loop for every command, enriching quantitative output with qualitative analysis. Qualitative findings are always attached to quantitative segments defined in the `/dps-setup` manifesto — never standalone per ARCH-02.

**Behavioral changes when quali mode is active:**

- **Anthropologist activation for all commands:** Stage 3 (Anthropologist) runs in the agent loop for every command — not just `/dps-inject-open`. The Anthropologist reads the current session context for any qualitative data (open-ended responses, interview transcripts, focus group notes) and attaches findings to the quantitative segments being analyzed. If no qualitative data is available in the session context, Stage 3 produces a "sem dados qualitativos disponíveis" notation and the loop falls through — the output is not degraded.
- **Theme enrichment in crosstabs:** When `/dps-cross` runs in quali mode, the output includes qualitative themes related to the crossed segments as margin notes. The Anthropologist identifies which themes are relevant to the specific cross being analyzed and the Tufte Designer weaves them into the margin notes as `"verbatim quote" — participant context` references that explain the "why" behind the quantitative pattern.
- **Full qualitative-to-quantitative mapping:** For `/dps-execute` in quali mode, each cross in the consolidated report includes Anthropologist findings. The Tufte Designer adds `### Análise Qualitativa — {Segmento}` subsections within each cross output, mirroring the `/dps-inject-open` output format but integrated into the execute flow.
- **Critic qualitative enforcement:** The Critic's qualitative audit dimension (dimension 7) activates for all commands: themes with &lt;2 verbatims are flagged as "menção isolada," Article 4 (percentage prohibition for N &lt; 30) is enforced on all output, overgeneralization from qualitative samples is blocked. Constitution enforcement does not relax in quali mode — it tightens.
- **Mandatory verbatim standards:** All qualitative output must include literal verbatim quotes (never paraphrased per agent-anthropologist.md), themes must have minimum 2 verbatims backing, and frequency is reported as raw counts ("mencionado por 8 de 12 participantes") — never percentages when N &lt; 30 per constitution.md Article 4.

**Usage:** If `/dps-mode:quali` is run as a standalone command, the persona activates for all subsequent commands in the current session. If used as a prefix (`/dps-mode:quali /dps-cross VarX x VarY`), the persona activates for that single command only — the mode does not persist beyond the prefixed command.

**Deactivation:** Run `/dps-mode:quant` to switch back to quantitative-only mode, deactivating the Anthropologist stage and returning to the default 3-stage loop (Statistician → Critic → Tufte Designer). Running `/dps-mode:quali` again while already in quali mode toggles off and returns to the default 3-stage loop.

Reference: the Anthropologist reads `agents/agent-anthropologist.md` for thematic categorization, verbatim extraction (literal quotes, minimum 2 per theme), frequency notation (raw counts, never % when N &lt; 30), archetype identification, and qualitative-to-quantitative segment mapping. The Critic reads `agents/agent-critic.md` qualitative audit dimension and `constitution.md` Articles 4 and 5 for enforcement.
```

**Verification of replacement:** After writing, confirm:
- The old 4-line stub text no longer appears in SKILL.md (the lines "Activates **Anthropologist** persona. Focus: latent needs..." should be gone, replaced by the session-scoped toggle statement and behavioral changes)
- The new content starts with `### /dps-mode:quali — Anthropologist Persona` and follows the pattern of the `/dps-mode:quant` section above it
- The section contains: session-scoped toggle statement, "Behavioral changes when quali mode is active" with 5 enumerated behaviors, Usage section (standalone vs prefix), Deactivation section, Reference section
- The content between `/dps-mode:quali` and the next `###` heading (currently `### /dps-mode:strategy`) contains no remnants of the stub text
  </action>
  <verify>
    <automated>grep -c "### /dps-mode:quali" SKILL.md | grep -v '^#' | test $(cat) -eq 1 && grep -c "Behavioral changes when quali mode is active" SKILL.md | grep -v '^#' | test $(cat) -eq 1 && grep -c "session-scoped toggle" SKILL.md | grep -v '^#' | test $(cat) -ge 2 && grep -c "desativar" SKILL.md | grep -v '^#' | test $(cat) -ge 1 && grep -c "/dps-mode:quant" SKILL.md | grep -v '^#' | test $(cat) -ge 3</automated>
  </verify>
  <done>
/dps-mode:quali is a full session-scoped toggle following the /dps-mode:quant pattern. The stub is fully replaced. Behavioral changes define: Anthropologist activation for all commands, theme enrichment in crosstabs, full quali-to-quanti mapping in /dps-execute, Critic qualitative enforcement, and mandatory verbatim standards. Usage defines standalone vs prefix behavior. Deactivation via /dps-mode:quant or re-toggling. Cross-references agents/agent-anthropologist.md for method details. The section sits between /dps-mode:quant and /dps-mode:strategy in the Modes section.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mode state persistence | `/dps-mode:quali` sets a session-scoped state that modifies agent loop behavior for all subsequent commands. No PII or sensitive data is involved — the toggle only activates an internal processing stage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Mode state within session | accept | Mode toggle is a prompt instruction, not mutable state. The AI reads the session context to determine mode — there is no stored state to tamper with. |
| T-03-05 | Denial of Service | Quali mode with no qualitative data | mitigate | When quali mode is active but no qualitative data exists in session, the Anthropologist stage produces "sem dados qualitativos disponíveis" and falls through without degrading output. The mode does not block or degrade quantitative analysis. |
</threat_model>

<verification>
Verify the plan by running: (a) `grep -c "### /dps-mode:quali" SKILL.md` returns 1, (b) the section between `/dps-mode:quali` and `/dps-mode:strategy` contains "Behavioral changes when quali mode is active", (c) the word "desativar" or deactivation instruction exists, (d) the old stub text "Activates **Anthropologist** persona. Focus: latent needs..." no longer appears inline — it's been replaced by the session-scoped toggle statement.
</verification>

<success_criteria>
- /dps-mode:quali section is a full session-scoped toggle, not a stub
- Behavioral changes section defines 5 specific changes (Anthropologist activation, theme enrichment, quali-to-quant mapping, Critic enforcement, verbatim standards)
- Usage section defines standalone vs prefix behavior per D-08
- Deactivation section documents how to return to default 3-stage loop
- Cross-references to agent-anthropologist.md, agent-critic.md, and constitution.md present
- Pattern follows /dps-mode:quant structure (lines 317-329) — same heading level, same level of detail
- Old stub text is fully replaced — no orphaned content between mode sections
</success_criteria>

<output>
Create `.planning/phases/03-qualitative-injection/03-02-SUMMARY.md` when done
</output>
