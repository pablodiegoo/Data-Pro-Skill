---
phase: 03-qualitative-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [SKILL.md]
autonomous: true
requirements: [INJECT-01, ARCH-02]

must_haves:
  truths:
    - "User can paste open-ended response text via /dps-inject-open and see themes extracted and categorized within existing quantitative segments from /dps-setup"
    - "Qualitative findings appear as ### Análise Qualitativa — {Segmento} subsections within each quant segment — never as standalone sections"
    - "Theme frequencies are reported as raw counts (e.g., 'mencionado por 8 de 12 participantes') — never percentages for N<30"
    - "Verbatims appear in literal quotes with participant context (e.g., — P4, 22 anos)"
    - "The invisible agent loop includes an Anthropologist stage (Stage 3) between Critic and Tufte Designer that activates when /dps-inject-open runs"
    - "The Critic audits qualitative output: themes with <2 verbatims flagged, overgeneralization blocked, N<30 percentage prohibition enforced (Articles 4, 5)"
  artifacts:
    - path: "SKILL.md"
      provides: "Anthropologist Stage 3 in Internal Agent Loop"
      contains:
        - "### Stage 3: Anthropologist"
        - "Qualitative Analysis"
        - "Runs when the command is /dps-inject-open OR when /dps-mode:quali is active"
    - path: "SKILL.md"
      provides: "/dps-inject-open command definition"
      contains:
        - "## Command: /dps-inject-open"
        - "### Execution Steps"
        - "### Output Format"
        - "### Análise Qualitativa — {Segmento}"
        - "Execute /dps-setup primeiro para definir os segmentos quantitativos"
    - path: "SKILL.md"
      provides: "Updated Command Reference table"
      contains:
        - "/dps-inject-open.*Phase 3.*implemented"
  key_links:
    - from: "/dps-inject-open Execution Steps"
      to: "Stage 3 Anthropologist (agent loop)"
      via: "Agent loop invocation in Step 2"
      pattern: "invisible agent loop.*Stage 1.*Stage 2.*Stage 3.*Stage 4"
    - from: "Anthropologist theme mapping"
      to: "/dps-setup manifesto segments"
      via: "Segment extraction in Step 1 validation"
      pattern: "segmentos definidos no manifesto"
    - from: "Anthropologist output"
      to: "Tufte Designer Stage 4"
      via: "Synthesis into quali subsections within quant segments"
      pattern: "Análise Qualitativa.*Segmento"
    - from: "Critic Stage 2 qualitative audit"
      to: "Constitution Articles 4 and 5"
      via: "Quali-specific audit dimensions"
      pattern: "temas com menos de 2 verbatims|generalização.*N < 30|porcentagem.*qualitativo"
---

## Phase Goal

**As a** market researcher, **I want to** paste open-ended response text and have the AI automatically extract themes, identify representative verbatims, and categorize them within the quantitative segments I defined with /dps-setup, **so that** my analysis gains explanatory depth with verbatim evidence — without ever creating standalone qualitative sections that break the quant-first architecture.

---

<objective>
Add the Anthropologist stage to the invisible agent loop (Stage 3, between Critic and Tufte Designer) and implement the `/dps-inject-open` command in SKILL.md. When invoked, the command runs the full 4-stage loop (Statistician → Critic → Anthropologist → Tufte Designer) and produces qualitative subsections (`### Análise Qualitativa — {Segmento}`) within each quantitative segment defined by the `/dps-setup` manifesto.

Purpose: Give the researcher the ability to enrich quantitative segments with verbatim-coded qualitative themes — theme extraction, verbatim selection, and segment mapping are all handled by the Anthropologist stage. No external CSV structuring required — the AI reads raw text and extracts themes automatically per D-01.

Output: SKILL.md updated with Anthropologist stage in agent loop, `/dps-inject-open` command specification, and updated Command Reference table.
</objective>

<execution_context>
@/home/pablo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pablo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@SKILL.md
@constitution.md
@agents/agent-anthropologist.md
@agents/agent-critic.md
@agents/agent-tufte-designer.md
@agents/agent-statistician.md
@.planning/phases/03-qualitative-injection/03-CONTEXT.md

<interfaces>
<!-- From SKILL.md — key anchors for insertion points -->
Current SKILL.md structure (lines approximate — verify with Grep before editing):
- Lines 20-73: ## Internal Agent Loop (Stage 1 Statistician → Stage 2 Critic → Stage 3 Tufte Designer)
- Line 66: End of Stage 2 Critic (closes with "---" on line 67)
- Line 68: ### Stage 3: Tufte Designer (needs to become Stage 4)
- Line 75: `---` separator before commands
- Lines 267-296: ## Command: /dps-execute (ends ~line 296)
- Line 297: ## Command Reference (## heading, starts the table)
- Lines 305-308: Command Reference table row for /dps-inject-open (status: "Full implementation in Phase 3")

Insertion points:
- A) Anthropologist stage: AFTER Stage 2 Critic (after line 66 `---`), BEFORE current Stage 3 Tufte Designer
- B) Renumber Stage 3 Tufte Designer → Stage 4
- C) /dps-inject-open command: AFTER /dps-execute section (after line 296), BEFORE ## Command Reference (line 297)
- D) Command Reference table: UPDATE /dps-inject-open row status to "Phase 3 — implemented"

Key existing patterns to follow:
- `/dps-cross` command (lines 212-265): Execution Steps structure with Stage 1 → Stage 2 → Stage 3, Output Format with table template, Constraints list
- `/dps-setup` command (lines 77-123): Output format with YAML frontmatter template, Format Rules list
- Anthropologist agent already loaded at agents/agent-anthropologist.md — the SKILL.md stage references its methods, does not duplicate
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Anthropologist Stage 3 to invisible agent loop and renumber Tufte Designer to Stage 4</name>
  <files>SKILL.md</files>
  <action>
Insert a new Stage 3: Anthropologist section into the Internal Agent Loop (lines 20-73 of SKILL.md) between the end of Stage 2 Critic and the current Stage 3 Tufte Designer. The insertion replaces the horizontal rule separator currently on line 67.

**Structure to insert (AFTER Stage 2 Critic, BEFORE Tufte Designer):**

Define Stage 3 with this exact heading and content pattern (matching Stage 1 and Stage 2 format):

```markdown
### Stage 3: Anthropologist (Qualitative Analysis)

Read `agents/agent-anthropologist.md` for detailed instructions.

**Activation:** This stage runs when (a) the command is `/dps-inject-open`, OR (b) `/dps-mode:quali` is active (see Modes section below). For quantitative-only commands without quali mode active, this stage is skipped — the loop falls through from Stage 2 Critic directly to Stage 4 Tufte Designer.

**Key responsibilities:**
1. Thematic categorization — read all open-ended responses, identify recurring words, concepts, emotions, and needs. Group into thematic clusters.
2. Verbatim extraction — select representative quotes that contain specific, concrete language and reveal the "why" behind quantitative patterns. Minimum 2 verbatims per reported theme (fewer = "menção isolada" and not reported as a theme).
3. Segment mapping — map every identified theme to a quantitative segment defined in the `/dps-setup` manifesto. Never create new segments — all themes attach to existing segments only.
4. Theme frequency — report as raw count within each segment (e.g., "mencionado por 8 de 12 participantes do Segmento A"). Never use percentages when N &lt; 30 per constitution.md Article 4.
5. Archetype identification — when patterns coalesce around consistent persona types, identify: archetype name, core need, pain point, and which quantitative segment they belong to.
6. Silence notation — if a theme expected from quantitative data does NOT appear in qualitative responses, note the absence. Absence of expected themes is a finding.

**Constitution reference:** Article 4 (N &lt; 30 no percentages) applies at this stage. Article 5 (parametric test minimums) is checked by the Critic.

Output format: Anthropologist Report (internal only — never shown to user). Contains Identified Themes, Verbatims, Segment Assignments, Archetypes, Emergent Patterns, and Noted Silences.
```

After inserting Stage 3, update the Tufte Designer heading from `### Stage 3` to `### Stage 4` and update its description to note it now synthesizes Anthropologist output alongside Statistician and Critic output.

**Critic enhancement (Stage 2):** Add a qualitative audit dimension to the Critic's Stage 2 responsibilities. After the existing "Prose Fluff Audit" line (dimension 6, currently line 56), add:

```
7. Qualitative Audit — When Anthropologist stage produced output: (a) flag any theme backed by &lt;2 verbatims as insufficient evidence, (b) flag any qualitative percentage claim applied to N &lt; 30 (Article 4 violation), (c) flag overgeneralization from qualitative samples to the full population, (d) flag confirmation bias where themes were selected to support a pre-existing hypothesis rather than emerging from the data.
```

This ensures the Critic enforces D-07 (themes with &lt;2 verbatims, overgeneralization, confirmation bias) whenever the Anthropologist produces output.
  </action>
  <verify>
    <automated>grep -n "### Stage 3: Anthropologist" SKILL.md && grep -n "### Stage 4: Tufte Designer" SKILL.md && grep -c "Qualitative Audit" SKILL.md | grep -v '^#' | test $(cat) -ge 1</automated>
  </verify>
  <done>
Agent loop has 4 stages: Stage 1 (Statistician), Stage 2 (Critic with qualitative audit dimension 7), Stage 3 (Anthropologist with activation rules and 6 responsibilities), Stage 4 (Tufte Designer). Anthropologist stage references agent-anthropologist.md, defines activation conditions (/dps-inject-open or /dps-mode:quali active), and covers thematic categorization, verbatim extraction with 2-verbatim minimum, segment mapping, raw-count frequency, archetypes, and silence notation.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement /dps-inject-open command specification and update Command Reference</name>
  <files>SKILL.md</files>
  <action>
Insert the full `/dps-inject-open` command section immediately after the `/dps-execute` command section (after its closing `---` on line 296) and BEFORE the `## Command Reference` heading (line 297). The insertion uses the same `---` separator pattern used between other commands.

**Command specification to insert:**

```markdown
## Command: /dps-inject-open [text] — Qualitative Injection into Quant Segments

**Purpose:** Categorize open-ended responses within existing quantitative segments defined by the `/dps-setup` manifesto. The AI auto-extracts themes from raw text (no structured CSV required per D-01), identifies representative verbatims, and maps findings to segments. Qualitative findings enrich — never replace or exist independently of — quantitative data. All output appears as subsections within quantitative segments, never as standalone sections (per ARCH-02).

### Execution Steps

1. **Validate manifesto.** Load the YAML frontmatter from the `/dps-setup` output. Extract the `segments` array and `sample_size`. If no manifesto exists in the current context, output the exact error message: "Execute `/dps-setup` primeiro para definir os segmentos quantitativos." Do not proceed with analysis. This gate ensures all qualitative findings have a quantitative segment to attach to per ARCH-02.

2. **Run the invisible agent loop (Stage 1 → 2 → 3 → 4).** Stage 3 (Anthropologist) activates for this command regardless of mode. The loop produces internal reports at Stage 1-3, and the Tufte Designer (Stage 4) synthesizes the only user-visible output.

   **Stage 1 — Statistician (internal, never shown to user):**
   For qualitative data, the Statistician's role is limited but important: (a) count the number of open-ended responses provided, (b) identify which segments have qualitative responses and which do not, (c) verify the response count per segment does not exceed the segment N from the manifesto (inconsistency flag), (d) report any segments from the manifesto that have zero qualitative responses as "sem dados qualitativos" for the Anthropologist to note as silence.

   **Stage 2 — Critic (internal, never shown to user):**
   Standard audit applies (all 6 constitution articles + Bias Detection, Spurious Correlations, Overgeneralization Risks). Additionally, the Qualitative Audit dimension (dimension 7) activates: (a) flag any theme backed by &lt;2 verbatims as "menção isolada — não reportar como tema", (b) flag any percentage applied to qualitative N &lt; 30 (Article 4 violation — hard block), (c) flag overgeneralization from qualitative sample to full population, (d) flag confirmation bias in theme selection.

   **Stage 3 — Anthropologist (internal, never shown to user):**
   Full responsibilities per agents/agent-anthropologist.md. Key outputs: (a) thematic clusters with verbatim evidence, (b) segment assignments for each theme, (c) archetype identification if patterns coalesce, (d) silence notation for manifesto segments with no qualitative data. Minimum 2 verbatims per theme (per constitution.md Article 4 enforcement + D-07 audit). Verbatims must be literal quotes in the participant's own words — never paraphrased.

   **Stage 4 — Tufte Designer (ONLY output user sees):**
   Synthesize Anthropologist report into qualitative subsections within the quantitative segment structure. Never create standalone qualitative sections. Never use prose fluff. Go straight to the data.

### Output Format

Output is organized by `/dps-setup` segment. Each segment that has qualitative findings receives a `###` subsection. Segments with zero qualitative data get a silence note. The output starts directly with the first segment subsection — no preamble, no "Here is your qualitative analysis:" throat-clearing.

```markdown
### Análise Qualitativa — {Segmento}

**Contexto Quantitativo:** {reference to /dps-setup segment — name, N, core metric}
**Respostas analisadas:** {n} de {N} participantes do segmento

#### Tema 1: {Nome do Tema} — mencionado por {n} de {N} participantes

&gt; "{verbatim literal — palavras exatas do participante, entre aspas}" — {participant context: e.g., P4, 22 anos}
&gt; "{segundo verbatim do mesmo tema}" — {participant context}

&gt; **Nota de Margem:** {1-3 frases conectando este tema ao padrão quantitativo observado no segmento. Explica o "porquê" por trás dos números. Nunca repete o que a tabela já mostra. Referencia a métrica core do segmento quando relevante.}

#### Tema 2: {Nome do Tema} — mencionado por {n} de {N} participantes

&gt; "{verbatim}" — {context}
&gt; "{verbatim}" — {context}

&gt; **Nota de Margem:** {interpretação conectando ao quantitativo}

### Análise Qualitativa — {Segmento sem dados}

**Respostas analisadas:** 0 de {N} participantes
&gt; **Nota de Margem:** Nenhum dado qualitativo disponível para este segmento. A ausência de respostas abertas neste grupo pode indicar menor engajamento ou barreira não identificada — investigar na próxima coleta.
```

**Segments without qualitative data:** Report the silence explicitly per agent-anthropologist.md rule "Note silence." The silence note must appear after all segments with data, grouped at the end.

**Multiple segments:** Each segment gets its own `### Análise Qualitativa — {Segmento}` subsection. Order follows the segment order in the `/dps-setup` manifesto — do not reorder.

### Format Rules

- **Theme naming:** Descriptive, not abstract. "Barreira de Preço" not "Fator Econômico." Use the participant's own language to name themes when possible.
- **Verbatim quoting:** Literal quotes in the participant's exact words, wrapped in `"quotes"`. Never paraphrase. Include context: participant identifier, demographic marker, or segment label.
- **Frequency notation:** Raw counts only — `mencionado por 8 de 12 participantes`. For N &lt; 30, never compute or display percentages per Article 4. For N ≥ 30 (rare in qualitative), percentages are permitted but must include margin of error per Article 1.
- **Minimum theme threshold:** 2 verbatims minimum to report as a theme. Single mentions are noted as "menção isolada" and grouped at the end under `### Menções Isoladas` — not promoted to full themes.
- **Margin notes:** Every theme block ends with a `&gt; **Nota de Margem:**` connecting the qualitative finding to the quantitative pattern. 1-3 sentences. Never repeat verbatim content. Explain "so what?" for the business.
- **No standalone sections:** All qualitative content is nested within `###` subsections keyed to manifesto segments. A top-level `## Análise Qualitativa` heading is FORBIDDEN — the Critic blocks it.
- **No prose fluff:** The first text after each heading is data, not throat-clearing. All 8 forbidden phrases from constitution.md Article 3 apply.

### Constraints

- `/dps-inject-open` REQUIRES a `/dps-setup` manifesto. Without it, output the single-line error and stop. No partial analysis without segment anchors.
- The Anthropologist reads ALL responses — not a sample, not the first N responses. Theme frequency counts are from the full response set within each segment.
- Verbatims must be literal quotes. Paraphrasing loses the participant's voice and is a Critic audit failure.
- Themes with &lt;2 verbatims are "menções isoladas" — reported collectively, not as individual findings.
- Never create new segments. All qualitative findings map to segments defined in the `/dps-setup` manifesto only.
- The output format is the specification — the Tufte Designer does not improvise structure beyond what is defined here.
- The agent loop runs in full for every `/dps-inject-open` invocation. No caching or incremental mode.
```

**Command Reference update:** In the Command Reference table (line 305-308), update the `/dps-inject-open` row. Change the Status column from "Full implementation in Phase 3" to "Phase 3 — implemented". Do not change any other rows or columns.

Verify the table update matches the existing format — same column alignment, same markdown table syntax.
  </action>
  <verify>
    <automated>grep -c "## Command: /dps-inject-open" SKILL.md | grep -v '^#' | test $(cat) -eq 1 && grep -c "### Execution Steps" SKILL.md | grep -v '^#' | test $(cat) -ge 7 && grep -c "### Output Format" SKILL.md | grep -v '^#' | test $(cat) -ge 6 && grep -c "Análise Qualitativa" SKILL.md | grep -v '^#' | test $(cat) -ge 3 && grep "dps-inject-open" SKILL.md | grep "Phase 3.*implemented"</automated>
  </verify>
  <done>
/dps-inject-open command is fully specified in SKILL.md with: prerequisite validation (manifesto gate with exact error message), 4-stage agent loop execution steps (Stat → Critic with quali audit → Anthrop → Tufte), output format as `### Análise Qualitativa — {Segmento}` subsections with themes, verbatims in quotes, raw-count frequencies, and margin notes connecting to quant patterns. Format rules cover theme naming, verbatim quoting, frequency notation, minimum 2-verbatim threshold, margin notes, and no-standalone enforcement. Constraints block analysis without manifesto and enforce literal quotes. Command Reference table updated to "Phase 3 — implemented".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → AI processing | Raw open-ended text provided by the user is processed by the Anthropologist agent. No PII handling required — the meta-prompt only processes data the user intentionally provides. |
| AI output → Document system | Qualitative findings are formatted as Markdown and embedded in the analytical document. No executable code is generated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | User-provided qualitative text | accept | Text is processed as-is; no injection vector exists in a Markdown meta-prompt system with no code execution sandbox |
| T-03-02 | Information Disclosure | Anthropologist internal report | accept | Internal agent reports are never shown to user — only Tufte Designer output is visible. No data leaves the AI context. |
| T-03-03 | Elevation of Privilege | Anthropologist segment mapping | mitigate | Anthropologist cannot create new segments — only maps to segments defined in /dps-setup manifesto. Critic enforcement prevents segment drift. |
</threat_model>

<verification>
Verify the plan by running: (a) `grep -c "Stage 3: Anthropologist" SKILL.md` returns 1, (b) `grep -c "Stage 4: Tufte Designer" SKILL.md` returns 1, (c) `grep -c "/dps-inject-open" SKILL.md` returns at least 2 (command heading + reference table), (d) `grep "dps-inject-open" SKILL.md | grep "implemented"` returns a line, (e) `grep -c "Análise Qualitativa" SKILL.md` returns at least 3.
</verification>

<success_criteria>
- Anthropologist stage (Stage 3) exists in the agent loop with activation logic (runs for /dps-inject-open OR when /dps-mode:quali active)
- Critic Stage 2 includes qualitative audit dimension 7 (themes <2 verbatims, Article 4 enforcement, overgeneralization)
- Tufte Designer is Stage 4 (renumbered from Stage 3)
- /dps-inject-open command section exists with Execution Steps (4-stage loop with Stage 3 activation), Output Format (`### Análise Qualitativa — {Segmento}`), Format Rules (min 2 verbatims, raw counts, literal quotes, margin notes), and Constraints
- Manifesto prerequisite gate outputs exact error message when /dps-setup hasn't been run
- Command Reference table shows /dps-inject-open status as "Phase 3 — implemented"
</success_criteria>

<output>
Create `.planning/phases/03-qualitative-injection/03-01-SUMMARY.md` when done
</output>
