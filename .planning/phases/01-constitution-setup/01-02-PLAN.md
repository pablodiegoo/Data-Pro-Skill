---
phase: 01-constitution-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - outputs/00_walking_skeleton_manifest.md
  - .planning/phases/01-constitution-setup/SKELETON.md
  - SKILL.md
  - constitution.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - CONST-01
  - OUTP-01
  - OUTP-02
  - HARN-01
  - HARN-02
  - ARCH-01
  - ARCH-03

must_haves:
  truths:
    - "A sample dataset processed through /dps-setup produces a valid Tufte-formatted manifesto"
    - "The manifesto output matches the exact format specifications from D-01 and D-02"
    - "The manifesto output contains zero forbidden prose fluff phrases"
    - "All 9 Phase 1 requirements are verifiably met by concrete artifacts"
    - "Architectural decisions are documented in SKELETON.md for all subsequent phases"
    - "SKILL.md passes zero-XML and zero-GSD-outside-file-paths validation"
  artifacts:
    - path: "outputs/00_walking_skeleton_manifest.md"
      provides: "Proof that /dps-setup works end-to-end with real sample data"
      min_lines: 25
      contains: "framework: \"Data-Pro-Skill v2\""
    - path: ".planning/phases/01-constitution-setup/SKELETON.md"
      provides: "Architectural decisions record — contract for all subsequent phases"
      contains: "Architectural Decisions"
      min_lines: 30
  key_links:
    - from: "outputs/00_walking_skeleton_manifest.md"
      to: "SKILL.md /dps-setup command"
      via: "execution of prompt instructions following the agent loop"
      pattern: "framework: \"Data-Pro-Skill v2\""
    - from: "outputs/00_walking_skeleton_manifest.md"
      to: "constitution.md Article 3"
      via: "fluff-free output verification"
      pattern: "> \\*\\*Nota de Margem"
    - from: "SKELETON.md Architectural Decisions"
      to: "SKILL.md + constitution.md"
      via: "documentation of chosen patterns for downstream phases"
      pattern: "Agent architecture"
</frontmatter>

<objective>
Prove the Data-Pro-Skill v2 meta-prompt works end-to-end by processing a real sample dataset through the full `/dps-setup` pipeline, validating the output against all 9 Phase 1 requirements, and recording architectural decisions in SKELETON.md — the contract for Phases 2, 3, and 4.

**Purpose:** The Walking Skeleton is the thinnest possible end-to-end proof. If this works, we know the architecture is sound.

**Output:** Walking skeleton manifest (`outputs/00_walking_skeleton_manifest.md`), architectural skeleton (`SKELETON.md`), fixes to SKILL.md/constitution.md if validation finds issues.
</objective>

<execution_context>
@/home/pablo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pablo/.config/opencode/get-shit-done/templates/summary.md
@/home/pablo/.config/opencode/get-shit-done/references/skeleton-template.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@SKILL.md
@constitution.md

@agents/agent-statistician.md
@agents/agent-critic.md
@agents/agent-tufte-designer.md

@.planning/phases/01-constitution-setup/01-CONTEXT.md
@.planning/phases/01-constitution-setup/01-RESEARCH.md
@.planning/phases/01-constitution-setup/01-01-SUMMARY.md

@.deprecated/v1/coonversa.md

<interfaces>
From SKILL.md /dps-setup command — the output format the walking skeleton must produce:

YAML frontmatter template:
```yaml
---
project: "{project_name}"
framework: "Data-Pro-Skill v2"
sample_size: {N}
metrics_tracked: [{metric1}, {metric2}, ...]
segments: [{seg1}, {seg2}, ...]
---
```

Markdown table template:
```
| Segmento | N | % | Métrica Core |
| :--- | :--: | :--: | :--- |
```

From constitution.md Article 3 — forbidden phrases that MUST NOT appear in output.
From agent-tufte-designer.md — margin note format: `> **Nota de Margem:** [1-3 sentences]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Walking Skeleton — run /dps-setup with sample data and produce manifesto output</name>
  <files>outputs/00_walking_skeleton_manifest.md</files>
  <read_first>
    - SKILL.md — read the FULL file, especially the /dps-setup command section and the agent loop instructions. You will act as the AI harness executing this prompt.
    - constitution.md — read ALL 6 articles. The Critic stage of your internal loop must enforce these.
    - agents/agent-statistician.md — read for Stage 1 instructions (sample profile, distributions, test selection, data quality)
    - agents/agent-critic.md — read for Stage 2 instructions (6 audit dimensions, bias detection, fluff scan)
    - agents/agent-tufte-designer.md — read for Stage 3 instructions (output formatting, forbidden phrases, margin notes)
    - .deprecated/v1/coonversa.md — read the v1 blueprint /setup output example (lines 95-118) for the expected output style
    - .planning/phases/01-constitution-setup/01-CONTEXT.md — re-read D-01 (YAML fields), D-02 (table columns), D-03 (no cross-tab map)
  </read_first>
  <action>
You are acting as an AI harness (like OpenCode) that has loaded the Data-Pro-Skill v2 meta-prompt. Follow these steps EXACTLY:

**Step A — Prepare sample dataset:**
Create the following synthetic market research dataset internally (do NOT write it as a separate file). This is a customer satisfaction survey for a SaaS product:

- Total N: 1450 respondents
- 3 segments: Promoters (NPS 9-10), Passives (NPS 7-8), Detractors (NPS 0-6)
- Segment breakdown: Promoters N=435 (30%), Passives N=580 (40%), Detractors N=435 (30%)
- Metrics tracked: NPS (overall: +8), CSAT (overall: 78%), Churn Risk (overall: 12%)
- Per segment: Promoters NPS=+72, CSAT=94%, Churn=2% | Passives NPS=+15, CSAT=76%, Churn=8% | Detractors NPS=-45, CSAT=52%, Churn=34%
- Demographics: 3 age groups, 2 regions — available for cross-tabulation in later phases
- Include some data quality notes: 2% missing on income question, 1 respondent with straight-lining on Likert block

**Step B — Execute the invisible agent loop (per SKILL.md instructions):**

Stage 1 (Statistician) — internally:
- Validate N consistency: Promoters(435) + Passives(580) + Detractors(435) = 1450 ✓
- Compute margin of error for each segment percentage: MoE = 1.96 × √(p(1-p)/1450)
  - For 30%: MoE = 1.96 × √(0.30 × 0.70 / 1450) = 1.96 × √(0.000145) = 1.96 × 0.01204 = ±2.4%
  - For 40%: MoE = 1.96 × √(0.40 × 0.60 / 1450) = 1.96 × √(0.000166) = 1.96 × 0.01287 = ±2.5%
- Flag the 1 straight-lining respondent
- Flag income variable at 2% missing (below 10% threshold — note but no block)
- Generate internal Statistician Report — DO NOT include in output

Stage 2 (Critic) — internally:
- Read constitution.md and check all 6 articles:
  - Article 1 (MoE): Verify MoE computed for each percentage → ✓
  - Article 2 (p<0.05): Not applicable to setup (no significance tests in manifesto) → ✓
  - Article 3 (Fluff): Prepare to scan output after Stage 3
  - Article 4 (Quali N<30): No qualitative data in setup → ✓
  - Article 5 (Parametric N): Not applicable to setup → ✓
  - Article 6 (Data Quality): Flag straight-lining and 2% missing income → note for margin
- Audit for bias: Selection bias — is sample representative? Note as caveat if demographic composition unknown
- Generate internal Critic Audit — DO NOT include in output

Stage 3 (Tufte Designer) — produce user-visible output as `outputs/00_walking_skeleton_manifest.md`:

**Step C — Write the manifest file with this exact structure:**

1. YAML frontmatter with ALL 5 D-01 fields filled with real values from the sample data:
   - `project: "Pesquisa de Satisfação SaaS — Q2 2026"`
   - `framework: "Data-Pro-Skill v2"` (exact string, hardcoded)
   - `sample_size: 1450`
   - `metrics_tracked: [NPS, CSAT, Churn Risk]`
   - `segments: [Promoters, Passives, Detractors]`

2. Level-1 heading: `# Pesquisa de Satisfação SaaS — Manifesto Quantitativo`

3. Anchor margin note: `> **Nota de Margem:** Este documento ancora o contexto numérico. Nenhuma análise posterior pode contradizer as métricas estabelecidas aqui.`

4. `## Matriz de Segmentos`

5. Table with EXACTLY 4 columns: Segmento | N | % | Métrica Core
   - Row 1: Promoters | 435 | 30% (±2.4%, 95% CI) | NPS: +72 / CSAT: 94%
   - Row 2: Passives | 580 | 40% (±2.5%, 95% CI) | NPS: +15 / CSAT: 76%
   - Row 3: Detractors | 435 | 30% (±2.4%, 95% CI) | NPS: −45 / CSAT: 52%

6. After the table, a margin note interpreting the data (NOT repeating the table):
   `> **Nota de Margem:** Detratores concentram a maior urgência — com NPS −45 e 34% de risco de churn, representam o segmento de intervenção prioritária. Promotores são a base de receita recorrente com apenas 2% de risco de churn.`

7. Additional section `## Métricas Gerais` with overall NPS, CSAT, and Churn, with MoE on percentages.

8. Data quality margin note (from Critic's constitution.md Article 6 findings):
   `> **Nota de Margem — Qualidade dos Dados:** 2% de missing na variável renda (abaixo do limiar de 10% do Article 6). 1 respondente com straight-lining detectado no bloco Likert — impacto desprezível em N=1450.`

**CRITICAL RULES:**
- ZERO prose fluff. First sentence after any heading goes straight to data.
- ALL percentages include margin of error: `X% (±Y%, 95% CI)`.
- Tables are self-explanatory — no surrounding paragraph needed to understand them.
- No emoji in output.
- All margin notes use `>` blockquotes with `**Nota de Margem:**` prefix.
- Do NOT include the Statistician Report or Critic Audit in the output file — those are internal only.
  </action>
  <acceptance_criteria>
1. `outputs/00_walking_skeleton_manifest.md` exists with at least 25 lines
2. YAML frontmatter contains ALL 5 fields from D-01: `project`, `framework` (exact value "Data-Pro-Skill v2"), `sample_size` (1450), `metrics_tracked` (array with at least 2 metrics), `segments` (array with at least 3 segments)
3. Segment matrix table has EXACTLY 4 columns: Segmento | N | % | Métrica Core
4. At least 3 segment rows in the table, each with a populated N, %, and Core Metric value
5. At least 2 percentages in the table include margin of error in format `X% (±Y%, 95% CI)` where Y is correctly computed: MoE = 1.96 × √(p(1-p)/1450)
6. At least 2 margin notes using `>` blockquote with `**Nota de Margem:**` prefix — one anchor note, one interpretive note
7. File contains `framework: "Data-Pro-Skill v2"` (exact string)
8. Zero forbidden prose fluff phrases from constitution.md Article 3 — verify with grep
9. Output starts with data/direct finding after any heading, not throat-clearing
10. No mention of "Statistician Report", "Critic Audit", or internal agent names in the output — only the Tufte Designer's formatted result is visible
11. Segment percentages (30%, 40%, 30%) sum to 100% (±0.5%)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'framework: "Data-Pro-Skill v2"' outputs/00_walking_skeleton_manifest.md && echo "---" && grep -c '| Segmento | N | % | Métrica Core |' outputs/00_walking_skeleton_manifest.md && echo "---" && grep -c 'Nota de Margem' outputs/00_walking_skeleton_manifest.md && echo "---" && grep -ci 'It is important to note\|Based on the data provided\|Interestingly,\|It is worth mentioning\|One can observe that\|The data suggests that\|In conclusion,\|As we can see' outputs/00_walking_skeleton_manifest.md && echo "---" && grep -c '±.*95% CI' outputs/00_walking_skeleton_manifest.md && echo "---" && grep -c 'sample_size:' outputs/00_walking_skeleton_manifest.md && echo "---" && grep -c 'metrics_tracked:' outputs/00_walking_skeleton_manifest.md && echo "---" && grep -c 'segments:' outputs/00_walking_skeleton_manifest.md</automated>
  </verify>
  <done>Walking skeleton manifest produced with valid YAML frontmatter (5 D-01 fields), 4-column segment table with 3+ rows, MoE on percentages, margin notes as blockquotes, zero fluff phrases</done>
</task>

<task type="auto">
  <name>Task 2: Validate all 9 requirements + create SKELETON.md + fix any issues</name>
  <files>
    .planning/phases/01-constitution-setup/SKELETON.md
    SKILL.md
    constitution.md
  </files>
  <read_first>
    - outputs/00_walking_skeleton_manifest.md (from Task 1 — the artifact being validated)
    - SKILL.md (the meta-prompt being validated for zero XML, DPS naming, command completeness)
    - constitution.md (being validated for article coverage)
    - .planning/ROADMAP.md — Phase 1 success criteria (lines 16-21) and requirements list
    - .planning/REQUIREMENTS.md — traceability matrix (lines 82-109) for all 9 Phase 1 requirement IDs
    - .planning/phases/01-constitution-setup/01-RESEARCH.md — Validation Architecture section (lines 601-635) with test map and Wave 0 gaps
    - .planning/phases/01-constitution-setup/01-CONTEXT.md — decisions D-01 through D-11 for requirement-to-decision traceability
    - @/home/pablo/.config/opencode/get-shit-done/references/skeleton-template.md
  </read_first>
  <action>

**Part A — Validate all 9 Phase 1 requirements:**

Run each check. If ANY check fails, fix the offending file and re-run until all pass.

| Req ID | Check | Command/Inspection |
|--------|-------|--------------------|
| SETUP-01 | /dps-setup manifesto has YAML frontmatter with segments, N, metrics | Verify `outputs/00_walking_skeleton_manifest.md` frontmatter has project, framework, sample_size, metrics_tracked[], segments[] |
| SETUP-02 | Manifesto table has N, %, Core Metric columns | Verify table header matches `| Segmento | N | % | Métrica Core |` |
| CONST-01 | constitution.md defines 8 rules across 6 articles | `grep -c '^## Article [1-6]' constitution.md` → 6 |
| OUTP-01 | Output has Tufte principles: data density, margin notes, self-explanatory tables | Verify manifest has `>` blockquote margin notes and self-explanatory table |
| OUTP-02 | No prose fluff phrases in output | `grep -ci '[forbidden phrases]' outputs/00_walking_skeleton_manifest.md` → 0 |
| HARN-01 | No XML tags in SKILL.md | `grep -c '<role>\|<constraints>\|<instructions>' SKILL.md` → 0 |
| HARN-02 | YAML frontmatter + pure Markdown in SKILL.md | Verify SKILL.md frontmatter has ONLY `name` + `description` fields |
| ARCH-01 | Quantitative-first pipeline described as spine | Verify SKILL.md contains "Quantitative-First" or equivalent architecture section |
| ARCH-03 | Invisible agent loop described (3 stages) | `grep -c 'Stage 1: Statistician' SKILL.md` → ≥1, same for Stage 2 + Stage 3 |

Additional cross-checks:
- DPS naming (D-10, D-11): `grep -v 'get-shit-done' SKILL.md | grep -ci 'gsd'` → 0
- All 9 /dps- commands present in SKILL.md: grep for each of /dps-setup, /dps-cross, /dps-inject-open, /dps-export, /dps-clarify, /dps-plan, /dps-mode:quant, /dps-mode:quali, /dps-mode:strategy → each ≥ 1
- constitution.md Article 3 fluff list completeness: verify at least 7 phrases are listed
- constitution.md Article 6 covers all 3 data quality rules: straight-lining, % sum=100%, missing >10%
- Walking skeleton manifest percentages sum to 100% (30 + 40 + 30 = 100) ✓

**If any check fails:** Fix the file immediately:
- SKILL.md missing a command → add to command reference table
- SKILL.md has "GSD" in text → replace with "DPS"
- constitution.md missing a rule → add article or expand existing article
- Walking skeleton manifest has fluff → rewrite the offending sentence
- Walking skeleton manifest missing MoE on a percentage → add `(±X%, 95% CI)`

**Part B — Create SKELETON.md:**

Create `.planning/phases/01-constitution-setup/SKELETON.md` following the skeleton template. Use these project-specific values:

Capability Proven End-to-End:
"A market researcher copies SKILL.md and constitution.md to their AI harness, invokes /dps-setup with raw quantitative survey data (N=1450, 3 segments), and the invisible agent loop (Statistician → Critic → Tufte Designer) produces a Tufte-formatted quantitative manifesto with YAML frontmatter, segment matrix with N/percentage/metric columns, margin notes as blockquotes, margin of error on all percentages, and zero prose fluff."

Architectural Decisions table:

| Decision | Choice | Rationale |
|---|---|---|
| Document format | Pure Markdown + YAML frontmatter | Universal across all 6 target harnesses (OpenCode, Gemini, Codex, Claude, Hermes, OpenClaw); no XML dependency; any harness can render Markdown |
| Agent architecture | Invisible 3-stage loop (Statistician → Critic → Tufte Designer) | Internal validation without user-facing complexity; user only sees final formatted output; each stage has a dedicated agent file in agents/ |
| Data anchoring | Document-driven: /dps-setup manifesto is single source of truth | Prevents context drift across analysis sessions; all subsequent commands reference manifesto segments; no analysis can contradict established metrics |
| Output style | Tufte principles: high data density, margin notes, self-explanatory tables | Maximizes information per word; tables include N and conclusions in headers; margin notes explain "so what?"; publication-ready output via Quarto/LaTeX |
| Command naming | /dps- prefix on all 9 commands | Distinguishes from GSD infrastructure; avoids harness-native command collision; consistent namespace for all analysis operations |
| Harness compatibility | Lowest common denominator: no XML, no HTML, no platform-specific syntax | Works on all 6 target harnesses without modification; agents/ files use XML internally but SKILL.md is pure Markdown |
| File structure | SKILL.md as orchestrator + constitution.md as rules + agents/ as detailed instructions | Concern separation: orchestration logic (SKILL.md) vs. enforcement rules (constitution.md) vs. agent-specific behaviors (agents/*.md); SKILL.md references others by path |
| Rule enforcement | constitution.md read by Critic agent; violations block output | Mechanical enforcement: each article defines a yes/no test the Critic can apply; violations prevent Designer from delivering output |
| Phase 1 scope | /dps-setup only; all other commands are named stubs | Vertical slice: one working command proves the architecture; other commands receive /dps- names and one-line descriptions for Phase 2+ implementation |

Stack Touched in Phase 1 checklist:
- [x] constitution.md — Statistical rigor rules (6 articles, 8 rules)
- [x] SKILL.md — Main meta-prompt: YAML frontmatter, agent loop, /dps-setup, command reference, Tufte rules, DPS naming
- [x] Agent loop — 3-stage invisible processing proven working (Statistician validates, Critic audits, Tufte Designer formats)
- [x] /dps-setup — Manifesto generation proven end-to-end with sample N=1450 dataset
- [x] Multi-harness format — Pure Markdown, zero XML, YAML frontmatter, no platform-specific syntax
- [x] Walking skeleton — output manifest at outputs/00_walking_skeleton_manifest.md

Out of Scope (Deferred to Later Phases):
- /dps-cross — Crosstab analysis with Tufte-style tables (Phase 2)
- /dps-clarify — Business hypothesis elicitation (Phase 2)
- /dps-plan — Analytical plan design (Phase 2)
- /dps-inject-open — Qualitative response categorization within segments (Phase 3)
- /dps-mode:quali full implementation — Anthropologist persona activation (Phase 3)
- /dps-export — Document consolidation + Quarto/LaTeX conversion (Phase 4)
- /dps-mode:strategy full implementation — BI Director persona (Phase 4)
- Multi-harness compatibility testing — validation across all 6 target harnesses (Phase 4)
- Automated installation scripts — per-harness setup automation (Phase 4)
- Quarto/LaTeX templates — PDF output pipeline (Phase 4)

Subsequent Slice Plan:
- Phase 2: /dps-clarify asks business questions, /dps-plan designs analytical approach, /dps-cross produces Tufte-style crosstabs with significance tests — all anchored to the Phase 1 manifesto
- Phase 3: /dps-inject-open categorizes open-ended responses within existing quantitative segments, /dps-mode:quali activates full Anthropologist persona — qualitative as a ramification of quantitative
- Phase 4: /dps-mode:strategy translates numbers into business recommendations, /dps-export consolidates everything into clean Markdown for Pandoc/Quarto/LaTeX, multi-harness validation across all 6 targets
  </action>
  <acceptance_criteria>
1. ALL 9 Phase 1 requirements (SETUP-01, SETUP-02, CONST-01, OUTP-01, OUTP-02, HARN-01, HARN-02, ARCH-01, ARCH-03) pass their validation checks from Part A
2. `grep -v 'get-shit-done' SKILL.md | grep -ci 'gsd'` returns 0 (zero GSD outside file paths)
3. `grep -c '<role>\|<constraints>\|<instructions>' SKILL.md` returns 0 (zero XML in SKILL.md)
4. All 9 /dps- commands are present in SKILL.md (verified by individual grep counts ≥ 1)
5. constitution.md has 6 articles each with all 4 sub-sections
6. Walking skeleton manifest has zero forbidden fluff phrases
7. Walking skeleton manifest has at least 2 percentages with MoE in `±X%, 95% CI` format
8. SKELETON.md exists in `.planning/phases/01-constitution-setup/SKELETON.md`
9. SKELETON.md contains ALL required sections: Capability Proven End-to-End (one sentence), Architectural Decisions (table with at least 7 rows), Stack Touched in Phase 1 (checklist with at least 5 items), Out of Scope (list with at least 5 items), Subsequent Slice Plan (Phases 2, 3, 4)
10. Any issues found during validation were fixed in the offending file(s) — SKILL.md, constitution.md, or walking skeleton manifest
  </acceptance_criteria>
  <verify>
    <automated>
# Run all 9 requirement checks
echo "=== SETUP-01: Manifesto YAML fields ==="
grep -c 'project:\|framework:\|sample_size:\|metrics_tracked:\|segments:' outputs/00_walking_skeleton_manifest.md
echo "=== SETUP-02: Table columns ==="
grep -c '| Segmento | N | % | Métrica Core |' outputs/00_walking_skeleton_manifest.md
echo "=== CONST-01: 6 articles ==="
grep -c '^## Article [1-6]' constitution.md
echo "=== OUTP-01: Margin notes ==="
grep -c 'Nota de Margem' outputs/00_walking_skeleton_manifest.md
echo "=== OUTP-02: Zero fluff ==="
grep -ci "It is important to note\|Based on the data provided\|Interestingly,\|It is worth mentioning\|One can observe that\|The data suggests that\|In conclusion,\|As we can see" outputs/00_walking_skeleton_manifest.md
echo "=== HARN-01: Zero XML in SKILL.md ==="
grep -c '<role>\|<constraints>\|<instructions>' SKILL.md
echo "=== HARN-02: YAML frontmatter ==="
grep -c '^name: data-pro-skill' SKILL.md
echo "=== ARCH-01: Quant-first pipeline ==="
grep -ci 'quantitative-first\|spine\|manifesto.*spine\|quant.*pipeline' SKILL.md
echo "=== ARCH-03: Agent loop stages ==="
grep -c 'Stage 1: Statistician' SKILL.md && grep -c 'Stage 2: Critic' SKILL.md && grep -c 'Stage 3: Tufte Designer' SKILL.md
echo "=== DPS naming: zero GSD outside paths ==="
grep -v 'get-shit-done' SKILL.md | grep -ci 'gsd'
echo "=== All 9 /dps- commands present ==="
for cmd in /dps-setup /dps-cross /dps-inject-open /dps-export /dps-clarify /dps-plan /dps-mode:quant /dps-mode:quali /dps-mode:strategy; do echo -n "$cmd: "; grep -c "$cmd" SKILL.md; done
echo "=== SKELETON.md exists ==="
test -f .planning/phases/01-constitution-setup/SKELETON.md && echo "EXISTS"
    </automated>
  </verify>
  <done>All 9 Phase 1 requirements validated, SKILL.md passes zero-XML and zero-GSD checks, SKELETON.md created with architectural decisions, any issues found were fixed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic sample data → /dps-setup execution | The walking skeleton uses fabricated data — validation checks are on format compliance, not data authenticity |
| SKILL.md instructions → Executor interpretation | The executor acting as AI harness may misinterpret prompt instructions — mitigation: explicit output format specification with examples |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Walking skeleton manifest (synthetic data passing as real) | accept | The manifest is explicitly synthetic sample output for validation purposes. Outputs/ clearly labeled as test artifacts. Real analysis uses user-provided data. |
| T-02-02 | Tampering | Validation checks (false positives) | mitigate | All validation checks are mechanical (grep counts, exact string matching). No subjective "looks correct" checks. Each check has an expected numeric output. |
| T-02-03 | Repudiation | SKILL.md fixes (gaps not tracked) | mitigate | Any fix applied during validation is documented in the SUMMARY.md change log. Git commit captures the diff. |
| T-02-SC | Tampering | N/A — no packages installed | accept | Phase 1 has zero external package dependencies. RESEARCH.md Package Legitimacy Audit confirms: pure Markdown + YAML. |
</threat_model>

<verification>
After both tasks complete, run this aggregate end-to-end verification:

```bash
echo "=== Phase 1 Complete Verification ==="

echo "1. constitution.md exists with 6 articles:"
grep -c '^## Article [1-6]' constitution.md

echo "2. SKILL.md has zero XML:"
grep -c '<role>\|<constraints>\|<instructions>' SKILL.md

echo "3. SKILL.md has zero GSD outside file paths:"
grep -v 'get-shit-done' SKILL.md | grep -ci 'gsd'

echo "4. Walking skeleton manifest exists and has correct format:"
grep -c 'framework: "Data-Pro-Skill v2"' outputs/00_walking_skeleton_manifest.md
grep -c '| Segmento | N | % | Métrica Core |' outputs/00_walking_skeleton_manifest.md
grep -c 'Nota de Margem' outputs/00_walking_skeleton_manifest.md

echo "5. Walking skeleton manifest has zero fluff:"
grep -ci "It is important to note\|Based on the data provided\|Interestingly,\|It is worth mentioning\|One can observe that\|The data suggests that\|In conclusion,\|As we can see" outputs/00_walking_skeleton_manifest.md

echo "6. Walking skeleton manifest has MoE on percentages:"
grep -c '±.*95% CI' outputs/00_walking_skeleton_manifest.md

echo "7. SKELETON.md exists with Architectural Decisions:"
grep -c 'Architectural Decisions' .planning/phases/01-constitution-setup/SKELETON.md

echo "8. All 9 /dps- commands in SKILL.md:"
for cmd in /dps-setup /dps-cross /dps-inject-open /dps-export /dps-clarify /dps-plan /dps-mode:quant /dps-mode:quali /dps-mode:strategy; do
  count=$(grep -c "$cmd" SKILL.md)
  echo "  $cmd: $count"
done

echo "=== Phase 1 Verification Complete ==="
```

Expected results:
1. 6
2. 0
3. 0
4. ≥1, ≥1, ≥2
5. 0
6. ≥2
7. ≥1
8. Each ≥1
</verification>

<success_criteria>
1. The walking skeleton manifest (`outputs/00_walking_skeleton_manifest.md`) proves the entire Data-Pro-Skill v2 pipeline works: raw data → invisible agent loop (Statistician → Critic → Tufte Designer) → Tufte-formatted quantitative manifesto
2. All 9 Phase 1 requirements are verifiably met — each check returns the expected value
3. SKILL.md contains zero XML tags and zero "GSD" references outside file paths — ready for multi-harness deployment
4. constitution.md has 6 enforceable articles covering all 8 rules from D-04 + D-05
5. SKELETON.md documents all architectural decisions — Phases 2, 3, and 4 have a clear contract to build against
6. The user can immediately use `/dps-setup` in any AI harness by copying just two files: SKILL.md + constitution.md
</success_criteria>

<output>
Create `.planning/phases/01-constitution-setup/01-02-SUMMARY.md` when done
</output>
