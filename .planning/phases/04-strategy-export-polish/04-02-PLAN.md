---
phase: 04-strategy-export-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - SKILL.md
autonomous: true
requirements:
  - HARN-03

must_haves:
  truths:
    - "SKILL.md contains zero XML tags anywhere in the file — verified by mechanical grep check"
    - "SKILL.md YAML frontmatter uses only harness-agnostic fields (name, description) — no tools, color, model, or platform-specific directives"
    - "All 10 command identifiers use the /dps- prefix consistently — no bare command names"
    - "SKILL.md has zero 'GSD' references outside get-shit-done file-path contexts"
    - "All agent file references (agents/agent-*.md) resolve to existing files"
    - "SKILL.md references constitution.md correctly for Critic enforcement"
    - "The full 10-command, 4-stage-loop SKILL.md is structurally compatible with OpenCode, Gemini, Codex, Hermes, OpenClaw, and Claude"
  artifacts:
    - path: "SKILL.md"
      provides: "Zero-XML validated document"
      contains: []
      note: "grep -c '<role>\\|<constraints>\\|<instructions>' SKILL.md must return 0"
    - path: "SKILL.md"
      provides: "Harness-agnostic YAML frontmatter"
      contains:
        - "name: data-pro-skill"
        - "description: |"
      note: "grep -c '^tools:\\|^color:\\|^model:' SKILL.md must return 0"
    - path: "SKILL.md"
      provides: "DPS-only naming (zero GSD outside file paths)"
      note: "grep -v 'get-shit-done' SKILL.md | grep -ci 'gsd' must return 0"
  key_links:
    - from: "SKILL.md Stage 2 Critic"
      to: "constitution.md"
      via: "Explicit file reference"
      pattern: "constitution\\.md"
    - from: "SKILL.md Stage 1 Statistician"
      to: "agents/agent-statistician.md"
      via: "Explicit file reference"
      pattern: "agent-statistician\\.md"
    - from: "SKILL.md Stage 3 Anthropologist"
      to: "agents/agent-anthropologist.md"
      via: "Explicit file reference"
      pattern: "agent-anthropologist\\.md"
    - from: "SKILL.md Stage 4 Tufte Designer"
      to: "agents/agent-tufte-designer.md"
      via: "Explicit file reference"
      pattern: "agent-tufte-designer\\.md"
    - from: "SKILL.md /dps-mode:strategy"
      to: "agents/agent-strategist.md"
      via: "Explicit file reference"
      pattern: "agent-strategist\\.md"
    - from: "SKILL.md Critic audit"
      to: "agents/agent-critic.md"
      via: "Explicit file reference"
      pattern: "agent-critic\\.md"
---

## Phase Goal

**As a** market researcher, **I want to** trust that the Data-Pro-Skill meta-prompt works identically across all 6 AI harnesses, **so that** I can deploy it to any platform without modification or compatibility debugging.

---

<objective>
Validate that the completed SKILL.md (10 commands, 4-stage agent loop, 2 mode specifications) is fully compatible with all 6 target AI harnesses: OpenCode, Gemini, Codex, Hermes, OpenClaw, and Claude. Run mechanical validation checks for XML contamination, YAML frontmatter compatibility, DPS naming consistency, and file reference integrity. Fix any issues found.

**Purpose:** HARN-03 is the final requirement of Phase 4 and the project. This validation ensures the prompt is truly harness-agnostic — the core design constraint of Data-Pro-Skill v2.

**Output:** SKILL.md validated and fixed (if issues found) — zero XML, clean YAML, DPS-only naming, all references intact.
</objective>

<execution_context>
@/home/pablo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/pablo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@SKILL.md
@constitution.md

@agents/agent-statistician.md
@agents/agent-critic.md
@agents/agent-anthropologist.md
@agents/agent-tufte-designer.md
@agents/agent-strategist.md

@.planning/phases/04-strategy-export-polish/04-CONTEXT.md

<interfaces>
HARN-03 validation dimensions — what constitutes "works on all 6 harnesses":

1. **Zero XML** — No `<role>`, `<constraints>`, `<instructions>`, or any XML tags in SKILL.md. Agent files in agents/ may use XML (they're internal), but SKILL.md must be pure Markdown.
2. **Minimal YAML frontmatter** — Only `name` and `description` fields. No `tools`, `color`, `model`, `parameters`, `schemas`, or any harness-specific directives.
3. **DPS naming** — All commands use `/dps-` prefix. No "GSD" references outside `get-shit-done/` path strings.
4. **Self-contained references** — All `agents/agent-*.md` and `constitution.md` paths resolve to existing files in the same directory.
5. **Pure Markdown syntax** — No HTML, no platform-specific Markdown extensions, no emoji in analytical output.
6. **Table syntax** — Standard Markdown tables with `:---` and `:--:` alignment — compatible with all 6 harnesses and Pandoc/Quarto.

Current SKILL.md state (after Plan 04-01): ~580 lines, 10 commands, 4-stage agent loop, 3 modes (quant/quali/strategy). SKILL.md has been progressively built across 4 phases — this is the final validation pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mechanical harness-compatibility validation suite and fix all issues</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md — FULL file (all ~580 lines). Read once, extract all patterns needed for validation.
    - .planning/REQUIREMENTS.md — HARN-01, HARN-02, HARN-03 descriptions for validation criteria
    - .planning/phases/04-strategy-export-polish/04-CONTEXT.md — inherited decisions: "zero XML, DPS naming, output Tufte"
  </read_first>
  <action>
Run the complete harness-compatibility validation suite against SKILL.md. Fix EVERY issue found — no partial passes, no warnings left unresolved.

**Validation Suite (run each check, fix if fail, re-run until all pass):**

### Check 1: Zero XML Tags (HARN-01)

```bash
grep -n '<role>\|<constraints>\|<instructions>\|<tags>\|<parameters>' SKILL.md
```

Expected: NO matches. Every match is an XML contamination that must be removed.

If any match found: Replace XML-style tags with Markdown equivalents:
- `<role>...</role>` → Use `### Role` heading + content
- `<constraints>...</constraints>` → Use `### Constraints` or `**Constraints:**` list
- `<instructions>...</instructions>` → Use `### Instructions` heading

**CRITICAL:** The agents/ directory files DO use XML — that is intentional and correct. Only SKILL.md must be XML-free. Do not modify agent files.

### Check 2: Harness-Agnostic YAML Frontmatter (HARN-02)

```bash
grep -n '^tools:\|^color:\|^model:\|^parameters:\|^schemas:' SKILL.md
```

Expected: NO matches. The YAML frontmatter should only have `name` and `description` fields.

Also verify:
```bash
grep -c '^name: data-pro-skill' SKILL.md
```
Expected: 1 (exactly one name field)

If any harness-specific field found: Remove it. Only `name` and `description` are permitted.

### Check 3: DPS Naming Consistency (HARN-03, inherited D-10/D-11)

Verify no "GSD" references outside file paths:
```bash
grep -n -v 'get-shit-done' SKILL.md | grep -i 'gsd'
```

Expected: NO matches. Every "gsd" or "GSD" occurrence must be inside a `get-shit-done/` path string.

If found: Replace "GSD" with "DPS" (e.g., "GSD infrastructure" → "project infrastructure", "GSD engine" → "DPS engine").

Verify all 10 command identifiers use `/dps-` prefix:
```bash
echo "=== All /dps- commands present ==="
for cmd in /dps-setup /dps-cross /dps-execute /dps-clarify /dps-plan /dps-inject-open /dps-export /dps-mode:quant /dps-mode:quali /dps-mode:strategy; do
  count=$(grep -c "$cmd" SKILL.md)
  echo "  $cmd: $count"
done
```

Expected: Each count ≥ 1.

Verify no bare command names (without /dps- prefix):
```bash
grep -n '^/\s*setup\>\|^/\s*cross\>\|^/\s*inject\>\|^/\s*export\>\|^/\s*clarify\>\|^/\s*plan\>' SKILL.md
```

Expected: NO matches. Every command reference must use the /dps- prefix.

### Check 4: File Reference Integrity

Verify all referenced files exist:
```bash
echo "=== File reference checks ==="
for ref in agents/agent-statistician.md agents/agent-critic.md agents/agent-anthropologist.md agents/agent-tufte-designer.md agents/agent-strategist.md constitution.md; do
  if test -f "$ref"; then echo "  ✓ $ref exists"; else echo "  ✗ $ref MISSING"; fi
done
```

Expected: All ✓. No missing files.

Verify SKILL.md references each agent file at least once:
```bash
echo "=== Agent file references in SKILL.md ==="
for agent in agent-statistician agent-critic agent-anthropologist agent-tufte-designer agent-strategist; do
  count=$(grep -c "agents/$agent\\.md" SKILL.md)
  echo "  agents/$agent.md: $count references"
done
```

Expected: Each count ≥ 1. Every agent must be referenced.

### Check 5: Pure Markdown Syntax

Verify no HTML tags in SKILL.md:
```bash
grep -n '<div\|<span\|<br\|&nbsp;\|style=' SKILL.md
```

Expected: NO matches. No HTML contamination.

Verify no emoji in analytical output sections (emojis in mode priority tiers like ⚡ are intentional and permitted — they're part of the prioritization matrix format). Check only the /dps-setup output template and /dps-cross output template:
```bash
# Check for emojis in command output templates (not in mode sections)
grep -n '⚡\|◆\|○' SKILL.md | grep -v 'mode:strategy'
```

If emojis found outside strategy mode's prioritization matrix: Remove them.

### Check 6: Command Reference Table Completeness

Verify the Command Reference table lists all commands with correct status:
```bash
grep -A1 '^| \`/dps-' SKILL.md | grep 'Status\|Phase'
```

Expected output should show:
- /dps-setup → Phase 1 — implemented
- /dps-cross → Phase 2 — implemented
- /dps-execute → Phase 2 — implemented
- /dps-clarify → Phase 2 — implemented
- /dps-plan → Phase 2 — implemented
- /dps-inject-open → Phase 3 — implemented
- /dps-export → Phase 4 — implemented

If any status is outdated or missing: Update it.

### Check 7: Agent Loop Integrity

Verify the agent loop structure is intact after all modifications:
```bash
echo "=== Agent loop stage count ==="
grep -c '^### Stage [1-4]:' SKILL.md
```

Expected: 4 (Statistician, Critic, Anthropologist, Tufte Designer).

Verify strategy mode does NOT appear as a stage:
```bash
grep -c 'Stage.*[Ss]trateg' SKILL.md
```

Expected: 0. Strategy must not appear as a stage in the agent loop.

### Check 8: Constitution Enforcement Chain

Verify the constitution enforcement chain is intact:
```bash
echo "=== Constitution references ==="
grep -c 'constitution\.md' SKILL.md
```
Expected: ≥ 3 (Critic stage, /dps-inject-open, and at least one other section).

### Fix Protocol

For every check that fails:
1. Identify the exact line(s) causing the failure
2. Fix the issue directly in SKILL.md using the Edit tool
3. Re-run the failing check to verify it passes
4. Move to the next check

Do NOT proceed to the next check until the current one passes. All 8 checks must pass before Task 1 is complete.
  </action>
  <verify>
    <automated>
#!/bin/bash
echo "=== HARN-03 Validation Suite ==="
echo ""

PASS=0
FAIL=0

check() {
  local name="$1"
  local cmd="$2"
  local expected="$3"
  local actual
  actual=$(eval "$cmd" 2>/dev/null)
  if [ "$actual" = "$expected" ]; then
    echo "  ✓ $name: $actual (expected $expected)"
    PASS=$((PASS + 1))
  else
    echo "  ✗ $name: got '$actual', expected '$expected'"
    FAIL=$((FAIL + 1))
  fi
}

echo "--- Check 1: Zero XML ---"
actual=$(grep -c '<role>\|<constraints>\|<instructions>' SKILL.md)
if [ "$actual" = "0" ]; then echo "  ✓ Zero XML: $actual tags"; PASS=$((PASS + 1)); else echo "  ✗ XML found: $actual tags"; FAIL=$((FAIL + 1)); fi

echo "--- Check 2: Harness-agnostic YAML ---"
actual=$(grep -c '^tools:\|^color:\|^model:' SKILL.md)
if [ "$actual" = "0" ]; then echo "  ✓ No harness-specific YAML fields: $actual"; PASS=$((PASS + 1)); else echo "  ✗ Harness-specific fields: $actual"; FAIL=$((FAIL + 1)); fi

echo "--- Check 3: DPS naming ---"
actual=$(grep -v 'get-shit-done' SKILL.md | grep -ci 'gsd')
if [ "$actual" = "0" ]; then echo "  ✓ Zero GSD outside paths: $actual"; PASS=$((PASS + 1)); else echo "  ✗ GSD found outside paths: $actual"; FAIL=$((FAIL + 1)); fi

echo "--- Check 4: File references ---"
all_exist=true
for ref in agents/agent-statistician.md agents/agent-critic.md agents/agent-anthropologist.md agents/agent-tufte-designer.md agents/agent-strategist.md constitution.md; do
  if test -f "$ref"; then echo "  ✓ $ref"; else echo "  ✗ $ref MISSING"; all_exist=false; fi
done
if $all_exist; then PASS=$((PASS + 1)); else FAIL=$((FAIL + 1)); fi

echo "--- Check 5: Pure Markdown ---"
actual=$(grep -c '<div\| style=' SKILL.md)
if [ "$actual" = "0" ]; then echo "  ✓ No HTML tags: $actual"; PASS=$((PASS + 1)); else echo "  ✗ HTML found: $actual"; FAIL=$((FAIL + 1)); fi

echo "--- Check 6: Command Reference completeness ---"
all_cmds=true
for cmd in /dps-setup /dps-cross /dps-execute /dps-clarify /dps-plan /dps-inject-open /dps-export; do
  count=$(grep -c "$cmd" SKILL.md)
  if [ "$count" -ge 1 ]; then echo "  ✓ $cmd: $count"; else echo "  ✗ $cmd: MISSING"; all_cmds=false; fi
done
if $all_cmds; then PASS=$((PASS + 1)); else FAIL=$((FAIL + 1)); fi

echo "--- Check 7: Agent loop integrity ---"
actual=$(grep -c '^### Stage [1-4]:' SKILL.md)
if [ "$actual" = "4" ]; then echo "  ✓ 4 agent loop stages: $actual"; PASS=$((PASS + 1)); else echo "  ✗ Expected 4 stages, got: $actual"; FAIL=$((FAIL + 1)); fi

echo "--- Check 8: Constitution chain ---"
actual=$(grep -c 'constitution\.md' SKILL.md)
if [ "$actual" -ge 3 ]; then echo "  ✓ Constitution references ≥ 3: $actual"; PASS=$((PASS + 1)); else echo "  ✗ Expected ≥3 references, got: $actual"; FAIL=$((FAIL + 1)); fi

echo ""
echo "=== Results: $PASS passed, $FAIL failed ==="
if [ "$FAIL" -gt 0 ]; then
  echo "FIX REQUIRED — resolve all failing checks before completing"
  exit 1
fi
    </automated>
  </verify>
  <done>All 8 harness-compatibility checks pass: zero XML, clean YAML, DPS-only naming, all file references intact, pure Markdown, Command Reference complete, 4-stage agent loop intact, constitution chain verified</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference final SKILL.md completeness — all 10 commands, all 4 phases</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md — post-Task-1 state (read full file for completeness audit)
    - .planning/ROADMAP.md — Phase 1-4 success criteria for cross-reference
    - .planning/REQUIREMENTS.md — all 21 v1 requirements and traceability matrix
  </read_first>
  <action>
Perform a final completeness audit of SKILL.md against all project requirements and phase deliverables. This is a structural read-only audit — fix only if something is broken by the Phase 4 modifications. Do not introduce new features or refactor.

**Audit dimensions:**

### A. Command Completeness (all 8 analysis commands + 3 modes)

Verify each has a complete specification with Execution Steps, Output Format, and Constraints:

| Command | Heading | Execution Steps | Output Format | Constraints | Status |
|---------|---------|-----------------|---------------|-------------|--------|
| /dps-setup | ✓ (Phase 1) | ✓ | ✓ | ✓ | — |
| /dps-clarify | ✓ (Phase 2) | ✓ | ✓ | ✓ | — |
| /dps-plan | ✓ (Phase 2) | ✓ | ✓ | ✓ | — |
| /dps-cross | ✓ (Phase 2) | ✓ | ✓ | ✓ | — |
| /dps-execute | ✓ (Phase 2) | ✓ | ✓ | ✓ | — |
| /dps-inject-open | ✓ (Phase 3) | ✓ | ✓ | ✓ | — |
| /dps-export | ✓ (Phase 4) | ✓ | ✓ | ✓ | Verify here |
| /dps-mode:quant | ✓ (Phase 2) | N/A (mode) | — | — | — |
| /dps-mode:quali | ✓ (Phase 3) | N/A (mode) | — | — | — |
| /dps-mode:strategy | ✓ (Phase 4) | N/A (post-processor) | — | — | Verify here |

For each incomplete command: note the gap and fix ONLY if the gap was introduced by Phase 4 modifications. Do not modify Phase 1-3 content unless Phase 4 changes broke something.

### B. Phase 4 Requirement Coverage

Verify EXPT-01, MODE-03, HARN-03 are fully covered:

```bash
echo "=== EXPT-01: /dps-export consolidates to single MD ==="
grep -c 'outputs/final_report\.md' SKILL.md

echo "=== MODE-03: /dps-mode:strategy BI Director persona ==="
grep -c 'BI Director' SKILL.md

echo "=== HARN-03: Compatibility verified (checks 1-8 pass) ==="
echo "Covered by Task 1 validation suite"
```

### C. Architecture Integrity

Verify the architectural principles are intact:

1. Quantitative-first pipeline: `grep -ci 'quantitative-first\|spine\|manifesto.*spine' SKILL.md` ≥ 1
2. Invisible agent loop: `grep -c '^### Stage [1-4]:' SKILL.md` = 4
3. Document-driven context: `grep -c '/dps-setup.*manifesto.*single source of truth\|manifesto.*ancora' SKILL.md` ≥ 1
4. Tufte formatting rules: `grep -c '^## Tufte Output Formatting Rules' SKILL.md` = 1

### D. Final Line Count

```bash
wc -l SKILL.md
```

Record the final line count. Should be approximately 580-620 lines after Phase 4 additions. If significantly different, investigate.

### E. Prose Fluff Scan

Verify the 8 forbidden phrases from constitution.md Article 3 do not appear in SKILL.md's own prose (not in the list of forbidden phrases itself):

```bash
grep -n 'It is important to note\|Based on the data provided\|Interestingly,\|It is worth mentioning\|One can observe that\|The data suggests that\|In conclusion,\|As we can see' SKILL.md | grep -v 'Forbidden\|proibida\|proibido'
```

Expected: NO matches outside the forbidden phrases list. If any found, rewrite the sentence.

**Fix protocol:** Only fix issues that would prevent a harness from correctly loading and executing SKILL.md. Cosmetic issues (wording, formatting preferences) are out of scope — note them in the SUMMARY but do not modify.
  </action>
  <verify>
    <automated>
#!/bin/bash
echo "=== Final Completeness Audit ==="

echo "--- Command headings present ---"
for cmd in "/dps-setup" "/dps-cross" "/dps-execute" "/dps-clarify" "/dps-plan" "/dps-inject-open" "/dps-export"; do
  count=$(grep -c "^## Command: $cmd" SKILL.md)
  echo "  $cmd: $count"
done

echo "--- Mode headings present ---"
for mode in "/dps-mode:quant" "/dps-mode:quali" "/dps-mode:strategy"; do
  count=$(grep -c "^### $mode" SKILL.md)
  echo "  $mode: $count"
done

echo "--- Phase 4 requirements ---"
echo -n "  EXPT-01 (exports/final_report.md): "
grep -c 'outputs/final_report\.md' SKILL.md
echo -n "  MODE-03 (BI Director): "
grep -c 'BI Director' SKILL.md
echo -n "  HARN-03 (compatibility): "
echo "covered by Task 1 validation"

echo "--- Architecture integrity ---"
echo -n "  Quant-first pipeline: "
grep -ci 'quantitative-first\|spine\|manifesto.*spine' SKILL.md
echo -n "  Agent loop stages: "
grep -c '^### Stage [1-4]:' SKILL.md
echo -n "  Tufte formatting rules: "
grep -c '^## Tufte Output Formatting Rules' SKILL.md

echo "--- Final line count ---"
wc -l SKILL.md

echo "--- Fluff scan (outside forbidden list) ---"
grep -n 'It is important to note\|Based on the data provided\|Interestingly,\|It is worth mentioning\|One can observe that\|The data suggests that\|In conclusion,\|As we can see' SKILL.md | grep -v 'Forbidden\|proibida\|proibido\|Deletion Test' || echo "  ✓ Zero fluff outside forbidden list"
    </automated>
  </verify>
  <done>Final completeness audit confirms all 10 commands present, all Phase 4 requirements covered, architecture intact, zero prose fluff in SKILL.md prose, final line count within expected range</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → AI Harness parser | The meta-prompt instructions cross the trust boundary when loaded by any of 6 harnesses — format contamination (XML, HTML, harness-specific syntax) could cause parse failures or undefined behavior |
| SKILL.md YAML frontmatter → Harness metadata parser | Frontmatter fields cross the trust boundary — harness-specific fields could conflict with the harness's own configuration system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | SKILL.md (XML contamination) | mitigate | Mechanical grep check for `<role>`, `<constraints>`, `<instructions>` — zero tolerance. Fix immediately if found. |
| T-04-08 | Denial of Service | SKILL.md (Harness parse failure from bad YAML) | mitigate | Validate YAML frontmatter contains ONLY `name` and `description` fields. Remove any `tools`, `color`, `model`, `parameters`, or `schemas` fields. |
| T-04-09 | Spoofing | SKILL.md (Bare command names colliding with harness commands) | mitigate | Verify all 10 commands use `/dps-` prefix. Check for bare `/setup`, `/cross`, `/inject`, `/export`, `/clarify`, `/plan` — none should exist in SKILL.md. |
| T-04-10 | Information Disclosure | SKILL.md (GSD references in prompt text) | accept | GSD references in file paths (`get-shit-done/`) are intentional and harmless on any harness. Non-path GSD references already mitigated by DPS rename. |
| T-04-11 | Elevation of Privilege | Agent files (XML in agents/ directory) | accept | Agent files intentionally use XML for internal clarity — this is documented in SKELETON.md. Harnesses never load agent files directly — they are read by SKILL.md's instructions. Acceptable risk. |
| T-04-SC | Tampering | N/A — no packages installed | accept | Phase 4 has zero external package dependencies. Validation is pure grep/text analysis. |
</threat_model>

<verification>
After both tasks complete, run the full harness-compatibility verification one final time:

```bash
echo "=== FINAL HARN-03 Verification ==="
echo ""

echo "1. Zero XML:"
grep -c '<role>\|<constraints>\|<instructions>' SKILL.md
echo "Expected: 0"

echo "2. Clean YAML frontmatter:"
grep -c '^tools:\|^color:\|^model:' SKILL.md
echo "Expected: 0"

echo "3. DPS naming (zero GSD outside paths):"
grep -v 'get-shit-done' SKILL.md | grep -ci 'gsd'
echo "Expected: 0"

echo "4. All agent files exist:"
for f in agents/agent-statistician.md agents/agent-critic.md agents/agent-anthropologist.md agents/agent-tufte-designer.md agents/agent-strategist.md constitution.md; do
  test -f "$f" && echo "  ✓ $f" || echo "  ✗ $f MISSING"
done

echo "5. No HTML contamination:"
grep -c '<div\| style=' SKILL.md
echo "Expected: 0"

echo "6. All 10 command IDs:"
for cmd in /dps-setup /dps-cross /dps-execute /dps-clarify /dps-plan /dps-inject-open /dps-export /dps-mode:quant /dps-mode:quali /dps-mode:strategy; do
  count=$(grep -c "$cmd" SKILL.md)
  echo "  $cmd: $count"
done
echo "Each expected: >= 1"

echo "7. Agent loop stages (must be 4):"
grep -c '^### Stage [1-4]:' SKILL.md
echo "Expected: 4"

echo "8. No strategy in loop:"
grep -c 'Stage.*[Ss]trateg' SKILL.md
echo "Expected: 0"

echo "9. Final SKILL.md line count:"
wc -l SKILL.md

echo ""
echo "=== HARN-03 Verification Complete ==="
```
</verification>

<success_criteria>
1. SKILL.md passes all 8 mechanical validation checks — zero failures
2. A researcher can copy ONLY SKILL.md, constitution.md, and the agents/ directory to any of the 6 target harnesses and all 10 commands work without modification
3. No XML tags exist in SKILL.md — verified by grep
4. YAML frontmatter contains only harness-agnostic fields (name, description)
5. All command identifiers use /dps- prefix consistently
6. All agent file references resolve to existing files
7. The project's architectural principles (quant-first, invisible loop, document-driven, Tufte output) remain intact after Phase 4 modifications
8. Zero prose fluff in SKILL.md's own instructional prose
</success_criteria>

<output>
Create `.planning/phases/04-strategy-export-polish/04-02-SUMMARY.md` when done
</output>
