---
phase: 04-strategy-export-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
autonomous: true
requirements:
  - EXPT-01
  - MODE-03

must_haves:
  truths:
    - "User can run /dps-export to produce a single consolidated Markdown file at outputs/final_report.md with YAML frontmatter"
    - "User can specify --manifest, --crosstabs, or --full flags to control which analysis sections are included in the export"
    - "Omitting the flag defaults to --full behavior (all analysis: manifesto + crosstabs + qualitative + strategy)"
    - "The exported file uses pure Markdown — zero XML tags — ready for Quarto/LaTeX/PDF conversion"
    - "User can run /dps-mode:strategy after completing analysis and receive 3-5 key business findings, a prioritization matrix (Impact x Effort), and a Monday morning action plan"
    - "/dps-mode:strategy operates as a standalone post-processor — it does NOT add a 5th stage to the invisible agent loop"
    - "/dps-mode:strategy references agents/agent-strategist.md for output formats: prioritization matrix, risk assessment, executive summary"
  artifacts:
    - path: "SKILL.md"
      provides: "/dps-export command specification"
      contains:
        - "## Command: /dps-export"
        - "### Execution Steps"
        - "### Output Format"
        - "--manifest"
        - "--crosstabs"
        - "--full"
        - "outputs/final_report.md"
        - "Quarto/LaTeX/PDF"
    - path: "SKILL.md"
      provides: "/dps-mode:strategy full specification replacing 4-line stub"
      contains:
        - "POST-PROCESSOR"
        - "**Key Business Findings (3-5)**"
        - "**Prioritization Matrix"
        - "**\"Monday Morning\" Action Plan**"
        - "Does NOT add a 5th stage"
        - "agents/agent-strategist.md"
    - path: "SKILL.md"
      provides: "Updated Command Reference table with /dps-export status"
      contains:
        - "/dps-export.*Phase 4.*implemented"
  key_links:
    - from: "/dps-export Execution Steps"
      to: "Session context (manifesto, crosstabs, qualitative, strategy)"
      via: "Step 1: Scan session context for all available analysis output"
      pattern: "Scan session context"
    - from: "/dps-export Output Format"
      to: "outputs/final_report.md"
      via: "Step 4: Write to outputs/final_report.md"
      pattern: "outputs/final_report\\.md"
    - from: "/dps-mode:strategy constraints"
      to: "agents/agent-strategist.md"
      via: "Explicit file reference for output formats"
      pattern: "agent-strategist\\.md"
    - from: "/dps-mode:strategy execution model"
      to: "Existing 4-stage agent loop"
      via: "Explicit exclusion: strategy runs AFTER the loop, not inside it"
      pattern: "does NOT add a 5th stage"
    - from: "/dps-export --full flag"
      to: "/dps-mode:strategy output"
      via: "Strategy section inclusion in full export"
      pattern: "strategy.*--full|--full.*strategy"
---

## Phase Goal

**As a** market researcher, **I want to** consolidate all analysis into a single publication-ready Markdown document and translate findings into actionable business recommendations, **so that** I can share results with stakeholders and act on insights without recompiling data.

---

<objective>
Add the `/dps-export` command (consolidated document export with flag-controlled sections) and expand the `/dps-mode:strategy` stub into a full post-processing specification (key findings, prioritization matrix, Monday morning action plan). These are the two "output" commands — export produces the final deliverable, strategy translates numbers into business action.

**Purpose:** Give the researcher the ability to produce a single clean Markdown file for Quarto/LaTeX/PDF conversion AND translate analysis findings into strategic recommendations — the final deliverables of the Data-Pro-Skill pipeline.

**Output:** SKILL.md updated with two new command implementations, updated Command Reference table, and an `/dps-mode:strategy` section that explicitly excludes itself from the invisible agent loop per D-08.
</objective>

<execution_context>
@/home/pablo/.config/opencode/dps-engine/workflows/execute-plan.md
@/home/pablo/.config/opencode/dps-engine/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/04-strategy-export-polish/04-CONTEXT.md

@SKILL.md
@agents/agent-strategist.md

<interfaces>
Key structures from existing files that the plan implementations must reference:

From agents/agent-strategist.md (already exists per D-09):
- Output dimensions: Key Business Findings (3-5 with evidence/implication/confidence), Prioritization Matrix (Impact x Effort table with priority tiers), Risk Assessment (probability/impact/mitigation), "Monday Morning" Action Plan (immediate + short-term + investigation), Executive Summary (max 4 sentences)
- Constraints: Never make recommendations unsupported by data, distinguish data findings from inferences, flag when quant and quali disagree, flag when recommendation requires additional research
- Role: "Take validated quantitative data and enriched qualitative insights, and answer one question: What do we do with this information on Monday morning?"

From SKILL.md current state (516 lines, Phase 3 complete):
- /dps-export command reference entry at line 406: status "Full implementation in Phase 4" → needs updating to "Phase 4 — implemented"
- /dps-mode:strategy stub at lines 445-448: 4-line placeholder → needs expansion to full specification
- Invisible agent loop: 4 stages (Statistician → Critic → Anthropologist → Tufte Designer) — strategy mode must NOT become Stage 5
- Insertion point for /dps-export command: between line 393 (end of /dps-inject-open constraints) and line 394 (--- separator before Command Reference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement /dps-export command specification with flag-controlled section export</name>
  <files>SKILL.md</files>
  <read_first>
    - .planning/phases/04-strategy-export-polish/04-CONTEXT.md — ALL decisions D-01 through D-05 for export behavior
    - SKILL.md — read from line 318 to 408 for insertion context (/dps-inject-open end → Command Reference table)
    - SKILL.md — read YAML frontmatter (lines 1-12) for current description to verify /dps-export already listed
    - SKILL.md — read Tufte Output Formatting Rules (lines 451-502) for formatting rules the export must inherit
  </read_first>
  <action>
Insert the `/dps-export` command specification into SKILL.md between the end of `/dps-inject-open` (line 393) and the `---` separator before the Command Reference table (line 394). The new command section replaces the `---` at line 394 — add a new `---` after the command section before the Command Reference heading.

Write the command specification following the EXACT pattern used by all existing commands (`## Command: /dps-{name} — {Description}`, `**Purpose:**`, `### Execution Steps`, `### Output Format`, `### Constraints`). Use pure Markdown — ZERO XML tags. All content in Portuguese (matching /dps-setup, /dps-inject-open, etc.) except code identifiers (`--manifest`, `--crosstabs`, `--full`, file paths).

**Structure:**

```
## Command: /dps-export — Exportação Consolidada do Documento

**Purpose:** Consolidar todas as análises (manifesto, cruzamentos, achados qualitativos, recomendações estratégicas) em um único arquivo Markdown limpo, pronto para conversão via Pandoc/Quarto/LaTeX/PDF.

### Execution Steps

1. **Escanear contexto da sessão.** Identificar todas as análises disponíveis na sessão atual: manifesto do /dps-setup, outputs de /dps-cross e /dps-execute, achados qualitativos do /dps-inject-open, e recomendações estratégicas do /dps-mode:strategy (se existirem). Se nenhuma análise existir, exibir: "Nenhuma análise disponível para exportação. Execute /dps-setup primeiro."

2. **Aplicar filtro da flag.** Baseado na flag fornecida:
   - `--manifest`: Incluir apenas o manifesto do /dps-setup
   - `--crosstabs`: Incluir apenas tabelas de cruzamento (/dps-cross e /dps-execute)
   - `--full`: Incluir TUDO (manifesto + cruzamentos + qualitativo + estratégia). **Comportamento padrão quando nenhuma flag é especificada** (per D-04).

3. **Montar documento.** Estruturar nesta ordem exata:
   a. YAML frontmatter com metadados do projeto (extraídos do manifesto /dps-setup)
   b. Seção do manifesto (se incluída pela flag)
   c. Seções de cruzamentos em ordem cronológica (se incluídas pela flag)
   d. Seções qualitativas, aninhadas dentro de seus segmentos (se incluídas pela flag)
   e. Seção de recomendações estratégicas (se incluída pela flag e output de estratégia existir)

4. **Escrever outputs/final_report.md.** Sobrescrever se existir. Usar Markdown puro — zero tags XML.

### Output Format

YAML frontmatter do documento exportado:
- `project`: herdado do manifesto /dps-setup
- `framework`: "Data-Pro-Skill v2" (hardcoded)
- `sample_size`: herdado do manifesto
- `metrics_tracked`: herdado do manifesto
- `segments`: herdado do manifesto
- `export_date`: data atual no formato YYYY-MM-DD
- `flags`: array com as flags utilizadas (ex: ["--full"])

Corpo do documento:
- Título nível 1: `# {Project Name} — Relatório Completo`
- Cada seção incluída preserva seu formato original Tufte (tabelas, notas de margem, verbatims)
- Seções aparecem em ordem cronológica (setup → crosses → quali → estratégia)
- Se uma seção não tem conteúdo (ex: --crosstabs mas nenhum cruzamento executado), incluir nota: "Nenhum cruzamento executado nesta sessão."

### Constraints

- Markdown puro — zero tags XML no output
- Sem prosa fluff — o documento exportado herda as regras de formatação Tufte (constitution.md Article 3, 8 frases proibidas)
- Tabelas preservam alinhamento Markdown (:--- para texto, :--: para números)
- Output pronto para Quarto/LaTeX/PDF — todas as tabelas usam sintaxe de alinhamento Markdown padrão
- A exportação NÃO re-executa análise — apenas consolida o que já existe no contexto da sessão
- Se a flag --full incluir estratégia mas /dps-mode:strategy não foi executado, incluir nota: "Recomendações estratégicas não disponíveis — execute /dps-mode:strategy para adicionar esta seção."
```

After inserting the command specification, update the Command Reference table entry for `/dps-export` (line 406): change status from "Full implementation in Phase 4" to "Phase 4 — implemented". Update the description to mention flags: "Consolidate all analysis outputs into a single clean Markdown file. Flags: --manifest, --crosstabs, --full (default). Ready for Pandoc/Quarto/LaTeX/PDF."

CRITICAL: The inserted command spec must cover ALL decisions D-01 through D-05:
- D-01: outputs/final_report.md ✓ (Step 4 + Output Format)
- D-02: YAML frontmatter with project metadata + chronological sections ✓ (Output Format)
- D-03: Flags --manifest, --crosstabs, --full ✓ (Step 2)
- D-04: No flag = --full default ✓ (Step 2)
- D-05: Quarto/LaTeX/PDF ready ✓ (Constraints)
  </action>
  <verify>
    <automated>
# Verify /dps-export command exists with all required sections
echo "=== /dps-export heading ==="
grep -c '^## Command: /dps-export' SKILL.md
echo "=== Execution Steps ==="
grep -c '^### Execution Steps' SKILL.md
echo "=== Output Format ==="
grep -c '^### Output Format' SKILL.md
echo "=== Constraints section ==="
grep -c '^### Constraints' SKILL.md
echo "=== Flag coverage ==="
grep -c '\-\-manifest' SKILL.md
grep -c '\-\-crosstabs' SKILL.md
grep -c '\-\-full' SKILL.md
echo "=== Default behavior (D-04) ==="
grep -c 'padrão quando nenhuma flag' SKILL.md
echo "=== Output file path (D-01) ==="
grep -c 'outputs/final_report\.md' SKILL.md
echo "=== Quarto/LaTeX/PDF (D-05) ==="
grep -c 'Quarto/LaTeX/PDF' SKILL.md
echo "=== Command Reference updated ==="
grep -c 'Phase 4.*implemented' SKILL.md
echo "=== Zero XML in new section ==="
awk '/^## Command: \/dps-export/,/^---$/' SKILL.md | grep -c '<role>\|<constraints>\|<instructions>'
    </automated>
  </verify>
  <done>/dps-export command spec inserted with Execution Steps, Output Format, and Constraints covering all D-01 to D-05 decisions. Command Reference updated to "Phase 4 — implemented". Zero XML tags.</done>
</task>

<task type="auto">
  <name>Task 2: Expand /dps-mode:strategy stub into full post-processing specification</name>
  <files>SKILL.md</files>
  <read_first>
    - .planning/phases/04-strategy-export-polish/04-CONTEXT.md — decisions D-06, D-07, D-08, D-09 for strategy behavior
    - agents/agent-strategist.md — FULL file (100 lines). Output dimensions: Key Business Findings, Prioritization Matrix, Risk Assessment, Monday Morning Action Plan, Executive Summary. Constraints: evidence-backed recs, distinguish data from inference, flag quant/quali disagreements, confidence indicators.
    - SKILL.md lines 445-448 — current 4-line /dps-mode:strategy stub to be expanded
    - SKILL.md lines 410-443 — /dps-mode:quant and /dps-mode:quali for mode specification pattern to follow
    - SKILL.md lines 20-93 — Internal Agent Loop section to understand the 4-stage loop that strategy must NOT join
  </read_first>
  <action>
Replace the current 4-line `/dps-mode:strategy` stub (SKILL.md lines 445-448) with a full specification following the pattern established by `/dps-mode:quant` and `/dps-mode:quali`. The expanded section must explicitly implement decisions D-06 through D-09 while preserving the existing text as the introduction.

Write the specification in pure Markdown — ZERO XML tags. Use Portuguese for section content (matching the existing mode sections), English only for code references and identifiers.

**Expanded content (replaces lines 445-448):**

```
### /dps-mode:strategy — BI Director Persona (Pós-Processamento)

Activates **BI Director** persona. Focus: translating numbers into actionable business recommendations. Unlike `/dps-mode:quant` and `/dps-mode:quali`, strategy mode is a **POST-PROCESSOR** — it runs AFTER all quantitative and qualitative analysis is complete, not as a stage in the invisible agent loop (per D-08).

**Execution model:** When `/dps-mode:strategy` is invoked:

1. **Read completed analysis.** Scan the current session context for all completed analysis: `/dps-setup` manifesto (segments, N, core metrics), all `/dps-cross` and `/dps-execute` outputs (crosstab tables with statistical tests), and all `/dps-inject-open` outputs (qualitative themes with verbatims). If no quantitative analysis exists, output the error: "Execute /dps-setup e pelo menos um comando de análise (/dps-cross ou /dps-execute) antes de ativar /dps-mode:strategy."

2. **Apply Strategist framework.** Read `agents/agent-strategist.md` for detailed output formats. The Strategist synthesizes ALL available evidence — quantitative patterns and qualitative verbatims — into business recommendations. Every recommendation must reference specific data or verbatim evidence. Never present opinion as fact — use confidence indicators.

3. **Produce strategic output.** The output follows the structure defined in `agents/agent-strategist.md` and is rendered by the Tufte Designer (Stage 4). Strategy runs OUTSIDE the agent loop — the Statistician, Critic, and Anthropologist stages are not invoked because the analysis is already complete and validated. The output IS the final recommendation document.

**Output dimensions (per agent-strategist.md and D-07):**

1. **Key Business Findings (3-5):** Distill everything into 3-5 high-impact findings. Each finding states a clear business implication, references specific data supporting it, and has a confidence level:

```
### Descoberta 1: [Finding]
**Evidência:** [specific metric from quantitative data]
**Implicação:** [what this means for the business]
**Confiança:** Alta (N={n}, ±{moe}% margem de erro) / Média / Baixa
```

2. **Prioritization Matrix (Impact × Effort):** Map findings to actionability:

| Ação Recomendada | Impacto | Esforço | Prioridade | Evidência |
| :--- | :--: | :--: | :--: | :--- |
| {Action} | Alto/Médio/Baixo | Alto/Médio/Baixo | ⚡ Imediata / ◆ Curto prazo / ○ Longo prazo | {data reference or verbatim} |

Priority tiers: ⚡ Imediata (high impact, low effort — do this Monday morning), ◆ Curto prazo (high impact, high effort OR medium impact, low effort — do this quarter), ○ Longo prazo (low impact, high effort — backlog).

3. **Risk Assessment:** Identify risks of acting AND risks of ignoring:

| Risco | Probabilidade | Impacto | Mitigação |
| :--- | :--: | :--: | :--- |
| {Risk if we act on this data} | Baixa/Média/Alta | Baixo/Médio/Alto | {mitigation strategy} |
| {Risk if we ignore this data} | Baixa/Média/Alta | Baixo/Médio/Alto | {mitigation strategy} |

4. **"Monday Morning" Action Plan:** Concrete next steps ordered by priority:

```
### Plano de Ação Imediato

1. **[Action]** — [who does what, based on which finding]
2. **[Action]** — [who does what]

### Próximos Passos (1-4 semanas)

1. **[Action]**
2. **[Action]**

### Investigação Adicional Necessária

- [Question the data raised but can't answer]
- [Additional data needed to confirm hypothesis]
```

5. **Executive Summary:** One paragraph (max 4 sentences) that a CMO or Director could read and understand the key takeaway. No methodology, no caveats — just the headline. Use business language, not statistical jargon.

**Tone and standards (from agent-strategist.md):**
- Direct and actionable — not academic, not theoretical
- Confidence-calibrated — do not oversell weak signals
- Business language — use terms the stakeholder uses, not statistical jargon
- Honest about uncertainty — flag what is clear vs what needs more investigation
- Distinguish between what the data shows vs what you infer
- If quantitative and qualitative disagree, say so — do not force alignment
- Flag when a recommendation requires additional research to confirm

**Constraints (per D-08 and D-06):**
- **Does NOT add a 5th stage to the invisible agent loop.** Strategy runs as a standalone post-processor — the agent loop remains Statistician → Critic → Anthropologist → Tufte Designer (4 stages). Strategy output is a separate deliverable, not a modification of the loop.
- Only operates after quantitative analysis is complete — validates that /dps-setup and at least one analysis command have been executed
- Every recommendation must reference specific data or verbatim evidence from the session context
- Never present opinion as fact — use confidence indicators (Alta/Média/Baixa)
- The strategy output can be included in `/dps-export --full` as a strategy section

**Reference:** The Strategist reads `agents/agent-strategist.md` for output formats including prioritization matrices (Impact × Effort), risk assessments, executive summaries, and action plan structures.
```

CRITICAL: The expanded section must enforce ALL decisions D-06 through D-09:
- D-06: "POST-PROCESSOR" label, "AFTER all quantitative and qualitative analysis is complete", "runs OUTSIDE the agent loop"
- D-07: Key Business Findings (3-5), Prioritization Matrix (Impact x Effort table), "Monday Morning" Action Plan (3 sub-sections)
- D-08: Explicit constraint "Does NOT add a 5th stage to the invisible agent loop" with explanation
- D-09: References "agents/agent-strategist.md" (file already exists, no creation needed)

The existing text "Activates **BI Director** persona. Focus: translating numbers into business recommendations..." should be preserved as the opening paragraph and expanded upon.
  </action>
  <verify>
    <automated>
# Verify /dps-mode:strategy expansion
echo "=== POST-PROCESSOR explicit (D-06) ==="
grep -c 'POST-PROCESSOR' SKILL.md
echo "=== Runs after analysis (D-06) ==="
grep -c 'AFTER all' SKILL.md
echo "=== Outside agent loop (D-06) ==="
grep -c 'OUTSIDE the agent loop' SKILL.md
echo "=== Key Business Findings (D-07) ==="
grep -c 'Key Business Findings' SKILL.md
echo "=== Prioritization Matrix (D-07) ==="
grep -c 'Prioritization Matrix' SKILL.md
echo "=== Monday Morning Action Plan (D-07) ==="
grep -c 'Monday Morning' SKILL.md
echo "=== Does NOT add 5th stage (D-08) ==="
grep -c 'Does NOT add a 5th stage' SKILL.md
echo "=== References agent-strategist.md (D-09) ==="
grep -c 'agents/agent-strategist\.md' SKILL.md
echo "=== Executive Summary ==="
grep -c 'Executive Summary' SKILL.md
echo "=== Risk Assessment ==="
grep -c 'Risk Assessment' SKILL.md
echo "=== BI Director persona preserved ==="
grep -c 'BI Director' SKILL.md
echo "=== Zero XML in expanded section ==="
awk '/^### \/dps-mode:strategy/,/^---$/' SKILL.md | grep -c '<role>\|<constraints>\|<instructions>'
echo "=== Error message for no analysis ==="
grep -c 'Execute /dps-setup e pelo menos um comando' SKILL.md
    </automated>
  </verify>
  <done>/dps-mode:strategy expanded from 4-line stub to full post-processing specification with 5 output dimensions, explicit POST-PROCESSOR designation, agent loop exclusion constraint, and agent-strategist.md reference. All D-06 through D-09 decisions enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session analysis data → Export assembler | Analyzed data crosses trust boundary during consolidation — export must faithfully represent, not selectively omit or misrepresent |
| Session analysis data → Strategist | Analyzed data crosses trust boundary during recommendation generation — strategist must not hallucinate unsupported claims |
| Export output → External system (Quarto/LaTeX) | Output file crosses trust boundary — must be structurally valid Markdown with correct YAML and table syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | /dps-export (Selective inclusion bias) | mitigate | Export spec Step 1 requires scanning ALL session context — no selective omission allowed. If a section has no content, include an explicit note rather than silently dropping it. |
| T-04-02 | Tampering | /dps-export (Output file overwrite) | accept | Overwrite behavior is documented and intentional (Step 4: "Sobrescrever se existir"). User is expected to manage file versions. Low risk — file is derived from session context, not a source of truth. |
| T-04-03 | Spoofing | /dps-mode:strategy (Hallucinated recommendations) | mitigate | Strategy spec requires "every recommendation must reference specific data or verbatim evidence." agent-strategist.md constraints enforce: never make recommendations unsupported by data, distinguish data from inference, flag when recommendation requires additional research. |
| T-04-04 | Information Disclosure | /dps-mode:strategy (Overstated confidence) | mitigate | Strategy spec requires confidence indicators (Alta/Média/Baixa) on every finding. agent-strategist.md constraints: "do not oversell weak signals" and "honest about uncertainty — flag what is clear vs what needs more investigation." |
| T-04-05 | Elevation of Privilege | /dps-mode:strategy (Unauthorized business decisions) | accept | Strategy provides RECOMMENDATIONS, not decisions. The role is advisory. Risk accepted — the user is the decision-maker; the AI only suggests. |
| T-04-06 | Tampering | /dps-export YAML frontmatter (Malformed YAML) | mitigate | Export spec uses fixed YAML template with typed fields from /dps-setup manifesto. No user-generated YAML is injected. YAML structure is deterministic. |
| T-04-SC | Tampering | N/A — no packages installed | accept | Phase 4 has zero external package dependencies. Pure Markdown + YAML prompt modifications only. |
</threat_model>

<verification>
After both tasks complete, run these aggregate checks:

```bash
echo "=== Phase 4 Plan 01 Aggregate Verification ==="

echo "1. /dps-export command exists with all sections:"
grep -c '^## Command: /dps-export' SKILL.md
grep -c '^### Execution Steps' SKILL.md
grep -c '^### Output Format' SKILL.md
grep -c '^### Constraints' SKILL.md

echo "2. All flags documented (D-03):"
grep -c '\-\-manifest' SKILL.md
grep -c '\-\-crosstabs' SKILL.md
grep -c '\-\-full' SKILL.md

echo "3. Default behavior (D-04):"
grep -c 'padrão quando nenhuma flag' SKILL.md

echo "4. Output file path (D-01):"
grep -c 'outputs/final_report\.md' SKILL.md

echo "5. Quarto/LaTeX/PDF readiness (D-05):"
grep -c 'Quarto/LaTeX/PDF' SKILL.md

echo "6. Command Reference updated:"
grep -c '/dps-export.*Phase 4.*implemented' SKILL.md

echo "7. Strategy mode is POST-PROCESSOR (D-06):"
grep -c 'POST-PROCESSOR' SKILL.md

echo "8. Strategy output dimensions (D-07):"
grep -c 'Key Business Findings' SKILL.md
grep -c 'Prioritization Matrix' SKILL.md
grep -c 'Monday Morning' SKILL.md

echo "9. No 5th stage (D-08):"
grep -c 'Does NOT add a 5th stage' SKILL.md

echo "10. References agent-strategist.md (D-09):"
grep -c 'agents/agent-strategist\.md' SKILL.md

echo "11. Zero XML in new sections:"
awk '/^## Command: \/dps-export/,/^---$/' SKILL.md | grep -c '<role>\|<constraints>\|<instructions>'
awk '/^### \/dps-mode:strategy/,/^---$/' SKILL.md | grep -c '<role>\|<constraints>\|<instructions>'

echo "=== Verification Complete ==="
```

Expected results:
1. 1, 1, 1, 1
2. ≥1, ≥1, ≥1
3. ≥1
4. ≥1
5. ≥1
6. ≥1
7. ≥1
8. ≥1, ≥1, ≥1
9. ≥1
10. ≥1
11. 0, 0
</verification>

<success_criteria>
A market researcher can:
1. Run `/dps-export` (or `/dps-export --full`) after completing analysis and receive a single consolidated Markdown file at `outputs/final_report.md` with YAML frontmatter and all sections in chronological order
2. Run `/dps-export --manifest` to export only the /dps-setup manifesto, or `/dps-export --crosstabs` to export only crosstab tables
3. Feed the exported file directly into Pandoc, Quarto, or LaTeX for PDF generation — no manual cleanup needed
4. Run `/dps-mode:strategy` after completing analysis and receive 3-5 key business findings with evidence, a prioritization matrix (Impact x Effort), a risk assessment, and a concrete Monday morning action plan
5. Trust that strategy recommendations are backed by specific data evidence and clearly distinguish between data findings and business inferences
6. Understand that `/dps-mode:strategy` is a post-processor — it does not change how subsequent analysis commands behave
</success_criteria>

<output>
Create `.planning/phases/04-strategy-export-polish/04-01-SUMMARY.md` when done
</output>
